# P3-T11 / P4-T16 / P5-T18 — Export BE: Coaches, Teams, Tournaments

- **Task IDs:** P3-T11, P4-T16, P5-T18
- **Branch:** `feat/p3-t11-p4-t16-p5-t18-export-be`
- **Started:** 2026-05-27
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
Add `GET /{resource}/export` (bulk/filtered) and `GET /coaches/{coach}/export` (individual) endpoints for Coaches, Teams, and Tournaments. All reuse the existing `ReportExport` generic class. Mirror the `MemberExportController` pattern exactly: `ids[]` overrides filters, `columns[]` picks columns.

### Boost MCP research
- `database-schema` coaches → columns confirmed: pno, full_name_hi, full_name_en, mobile, nis_certified, member_id
- `database-schema` teams → columns confirmed: name_hi, session_id, sport_id, unit_id, in_charge_hi; no player/coach count column (use withCount)
- `database-schema` tournaments → columns confirmed: name_hi, session_id, tier_id, sport_id, venue, date_from, date_to; events via hasMany
- Skills consulted: laravel-best-practices

**Doc drift found:** None.

### Files to touch
- `app/Http/Controllers/CoachExportController.php` (new)
- `app/Http/Controllers/TeamExportController.php` (new)
- `app/Http/Controllers/TournamentExportController.php` (new)
- `routes/web.php` (add 4 routes)
- `tests/Feature/CoachExportTest.php` (new)
- `tests/Feature/TeamExportTest.php` (new)
- `tests/Feature/TournamentExportTest.php` (new)

---

## Implementation (Gate 2)

<!-- filled in automatically -->
