# P1-T35 — Org-scope leakage tests

- **Task ID:** P1-T35
- **Phase file:** [`docs/phases/P01-foundation.md`](../P01-foundation.md)
- **Branch:** `test/p1-t35-org-scope-leakage`
- **Started:** 2026-05-22
- **Status:** complete

---

## Plan (Gate 1)

### Scope
"Pest: org-scope leakage tests (user from org A cannot read org B data)."
Proves that the `BelongsToOrganization` global scope prevents cross-tenancy data access via HTTP routes for
the three org-scoped reference-data resources: SportSession, Sport, Unit.
Districts and TournamentTiers are global (no `organization_id`) and are excluded from leakage tests by design.

### Boost MCP research (mandatory)
- `database-schema` table=`sport_sessions` → confirms `organization_id` FK present
- `database-schema` table=`sports` → confirms `organization_id` FK present
- `database-schema` table=`units` → confirms `organization_id` FK present
- `database-schema` table=`districts` → NO `organization_id` — global resource, excluded
- `database-schema` table=`tournament_tiers` → NO `organization_id` — global resource, excluded
- Reviewed `app/Models/Scopes/BelongsToOrganization.php` — scope filters on `auth()->user()?->organization_id ?? 0`

**Doc drift found:** None.

### Files to touch
- `tests/Feature/OrgScopeLeakageTest.php` — new: all leakage assertions

### Risk + mitigation
- **Risk:** Route model binding may resolve by PK before global scope applies, letting Org B user access Org A model.
  **Mitigation:** The scope is applied at Eloquent query level; route model binding uses `Model::find()` which goes through the scope. If the model isn't found, Laravel returns 404. Tests verify this.

### Test plan
Per org-scoped resource (SportSession, Sport, Unit):
1. `GET index` — Org B user's collection is empty (Org A data not leaked)
2. `GET edit` — Org B user requesting Org A model → 404
3. `PATCH update` — Org B user → 404
4. `DELETE destroy` — Org B user → 404

Plus one global sanity check: Org B user can still see their own records on the index.

---

## Summary (Gate 3)

### What changed
- **`app/Models/Sport.php`** — added `Tenanted` trait (wires `BelongsToOrganization` global scope)
- **`app/Models/SportSession.php`** — added `Tenanted` trait
- **`app/Models/Unit.php`** — added `Tenanted` trait
- **`tests/Feature/OrgScopeLeakageTest.php`** — new: 13 tests / 45 assertions covering index isolation + edit/update/destroy 404s for all 3 org-scoped resources
- **`tests/Feature/Settings/SportCrudTest.php`** — cross-org update/destroy assertions updated 403 → 404
- **`tests/Feature/Settings/SportSessionCrudTest.php`** — same
- **`tests/Feature/Settings/UnitCrudTest.php`** — same
- **`tests/Feature/SportSeederTest.php`** — added `withoutGlobalScope(BelongsToOrganization::class)` helper functions; seeder tests have no authenticated user so the scope must be bypassed
- **`tests/Feature/SportSessionSeederTest.php`** — same

### Bug discovered and fixed
The `Tenanted` trait (and `BelongsToOrganization` scope) existed and was proven in `TenantedTraitTest.php`, but `Sport`, `SportSession`, and `Unit` never applied it. Route model binding resolved cross-org models by PK with no scope filter, allowing full cross-org read/write/delete. Now all three models boot the scope — route model binding returns 404 for out-of-scope records.

### Decisions
- **403 → 404 for cross-org access:** Route model binding with the scope in place returns 404 before the Gate runs. This is the correct and more secure behavior (doesn't reveal resource existence). CRUD tests updated to reflect reality.
- **Seeder tests bypass scope:** Seeder tests run without an authenticated user; `auth()->user()` is null → scope filters to `organization_id = 0` → all rows hidden. Correct fix is `withoutGlobalScope` since these tests verify seeder correctness, not tenant isolation.
- **Districts & TournamentTiers excluded:** they have no `organization_id` — leakage tests don't apply.

### Tests added
- `tests/Feature/OrgScopeLeakageTest.php` — 13 tests / 45 assertions

### Deferred / follow-ups
- `RbacServiceTest` (7 unit tests) and `LocaleTest` (3 tests) are pre-existing failures unrelated to this task.


### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [ ] Authorization gate / Policy added or updated — N/A (test only)
- [x] No new hard-coded user-facing strings — N/A

---

## Approval (Gate 5)

<!--
## Approved — 2026-05-22 by user
**PR:** #43
**Squash commit:** `test(tenancy): org-scope leakage — cross-org 404 on every org-scoped resource`
**Merged:** 2026-05-22
-->
