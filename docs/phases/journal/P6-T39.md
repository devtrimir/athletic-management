# P6-T39 — Coach playing-career achievements + print/quick-view wiring

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only.

- **Task ID:** P6-T39
- **Phase file:** coach polish (P6, journal-tracked)
- **Branch:** `feat/p6-t39-coach-playing-achievements`
- **Started:** 2026-09-05
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
(1) Add the coach's OWN playing-career achievements (from when the coach was a player) — stored in a standalone table so they are **completely isolated from every medal tally** (all tallies read the `achievements` table via `participations`; this table must never join those queries). Displayed as a new "Playing career achievements" section inside the existing Achievements tab, with full CRUD. (2) Wire BOTH the new playing achievements AND the P6-T38 special achievements into the coach print preview page and the coach quick-view modal (data + print HTML). User decisions: section inside Achievements tab; quick view included.

### Boost MCP research (mandatory)
- `search-docs` done for P6-T38 (same patterns apply: FormRequest validation, policy gates, nested route binding).
- Prior exploration (agent-12/agent-13): all medal aggregates (MedalTallyReport, MedalsByMemberReport, MedalsDetailReport, AchievementHistoryReport, DashboardController:135-162, TournamentProfileData, MemberController aggregates, PlayerPointsService) query `achievements` via `participations` — a separate `coach_playing_achievements` table is automatically excluded. Donor shape: `incharge_achievements` / `App\Models\InchargeAchievement` (standalone: title, period, level, competition_details, event_date, venue, sport_discipline, medal_type, position, achieved_on, remarks). Coach achievements tab payload: `CoachProfileData::achievementsPayload` (:236-390) — the playing section must NOT merge into `coachAchievements` (that payload feeds promotions reward-evidence options at :393-457; keep separate payload key).
- Skills consulted: `.agents/skills/laravel-best-practices/SKILL.md`, `.agents/skills/validation-audit-guardrails/SKILL.md`, `.agents/skills/pest-testing/SKILL.md`, `.agents/skills/product-ui-ux-standards/SKILL.md`.

**Doc drift found:** None.

### Files to touch
- `database/migrations/2026_09_05_000002_create_coach_playing_achievements_table.php` — org_id FK cascade, coach_id FK cascade, level/tier, title, competition_details, medal_type, position, event_date, venue, sport discipline, remarks; timestamps + org-scoped indexes (exact column set follows the incharge_achievements donor)
- `database/factories/CoachPlayingAchievementFactory.php`
- `app/Models/CoachPlayingAchievement.php` + `Coach::playingAchievements()` relation
- `app/Http/Controllers/CoachPlayingAchievementController.php` — store/update/destroy
- `app/Http/Requests/Coaches/StoreCoachPlayingAchievementRequest.php` / `Update...` — validation
- `app/Policies/CoachPolicy.php` — `managePlayingAchievements` (follow coach `manage*` convention with `coaches.update` fallback)
- `config/rbac.php` — permission (bilingual)
- `app/Support/Coaches/CoachProfileData.php` — add `playingAchievements` to `achievements()` tab payload (separate key from `coachAchievements`); add `specialAchievements` + `playingAchievements` to `print()`
- `routes/web.php` — `coaches.playing-achievements.*` routes
- `app/Services/AuditLogBuilder.php` — coach playing-achievement mappings
- `resources/js/pages/coaches/show.tsx` — "Playing career achievements" section inside the Achievements `<TabsContent>` with add/edit dialog + delete
- `resources/js/pages/coaches/print-preview.tsx` — two new toggleable sections (special achievements; playing achievements) following the existing Section/SectionKey pattern
- `app/Http/Controllers/Api/V1/CoachPreviewController.php` — add both payloads
- `resources/js/components/teams/coach-quick-view.tsx` — render both sections + include in `buildPrintHtml()`
- `resources/lang/en.json` + `hi.json` — new keys both locales
- `tests/Feature/CoachPlayingAchievementControllerTest.php` — new
- existing print/preview tests — extend for print payload + preview API payload

### Schema / endpoints / pages
- Migration: `create_coach_playing_achievements_table` — standalone, NOT linked to `achievements`/`participations` in any way
- Endpoints: `POST/PATCH/DELETE coaches/{coach}/playing-achievements[/{playingAchievement}]`, named `coaches.playing-achievements.*`; data rides the existing `coaches/{coach}/achievements` tab route
- Pages: `coaches/show.tsx` (achievements tab section), `coaches/print-preview.tsx` (2 sections), `coach-quick-view.tsx` (2 sections)

### Risk + mitigation
- **Risk:** playing achievements accidentally feed medal tallies. **Mitigation:** separate table, no relations to achievements/participations; test asserts `Achievement` count unchanged after store.
- **Risk:** promotions reward-evidence (`rewardEvidenceOptionsPayload`) picking up playing records. **Mitigation:** keep separate payload key; evidence query untouched.
- **Risk:** dev DB reset. **Mitigation:** never migrate/seed/rollback dev DB (pgsql `uppscb1`); tests use sqlite :memory:.

### Test plan
- Pest Feature: permission 403, store/update/destroy, validation, cross-coach/cross-org 404, tab payload includes playing achievements separate from coached summary, print payload includes both new sections, preview API includes both, tally-isolation assertion
- Translation audit: new keys in en.json AND hi.json

> Plan approved by user decisions in conversation (2026-09-05): section inside Achievements tab; two-PR split; quick view included.

---

## Summary (Gate 3) — fill after implementation

### What changed
- `database/migrations/2026_09_05_120000_create_coach_playing_achievements_table.php` — idempotent, mirrors incharge_achievements column set; zero relations to achievements/participations.
- `CoachPlayingAchievement` model + factory + `Coach::playingAchievements()`; controller + Store/Update requests; `coaches.playing-achievements.*` routes; `CoachPolicy::managePlayingAchievements` (permission OR `coaches.update`); rbac permission; AuditLogBuilder mappings.
- `CoachProfileData`: `achievements()` tab gains separate `playingAchievements` key; `print()` includes `specialAchievements` + `playingAchievements`.
- `resources/js/components/coaches/playing-achievements-section.tsx` rendered inside the Achievements tab of `coaches/show.tsx`.
- `coaches/print-preview.tsx`: two new toggleable sections; `CoachPreviewController` + `coach-quick-view.tsx` (body + buildPrintHtml) include both.
- en.json + hi.json: 11 new keys both locales (+ en.json backfill for hi-only keys).

### Decisions
- `period`/`event_date`/`competition_details` nullable — playing-career records may predate exact metadata.
- No client-side permission gating on edit UI (matches certifications/special-achievement tabs; server Gate enforced).

### Definition of Done
- [x] Tests pass (21/21 new+extended, 120 assertions, incl. tally-isolation; only pre-existing coach failures remain)
- [x] Pint / build / types:check clean for touched files
- [x] Translation audit: new keys in en.json AND hi.json
- [x] Dev DB untouched

## Status: awaiting-approval — PR #192 opened 2026-09-05

## Revision 1 — 2026-09-05
**Requested by user:** "for taking information for sport we can use the existing sports listing; then why only title is required no validation on dates; and for venue I want in full width"

**Changes:** (1) sport free-text replaced with sports-master select (sport_id FK); (2) event_date required with date validation, achieved_on date-validated; (3) venue field full-width in the dialog.

## Approved — 2026-09-05 by user

- PR #192 merged (merge commit `9150bb02`), branch deleted.
- Note: the P6-T40 commit was pushed to this branch seconds after merge and never entered main — re-landed via PR #193.
