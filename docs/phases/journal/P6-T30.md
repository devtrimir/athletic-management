# P6-T30 — Remove NIS certified check from Coach module

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only: never delete old sections, never rewrite history.

- **Task ID:** P6-T30
- **Phase file:** P6 (maintenance/refactors during import phase; no dedicated phase file entry yet)
- **Branch:** `feat/p6-t30-remove-coach-nis`
- **Started:** 2026-09-04
- **Status:** planning

---

## Plan (Gate 1)

### Scope
Remove the `nis_certified` boolean check (and the unused `nis_master_id` / "NIS info" field) from the Coach module everywhere: DB column, model, form requests, resources, controllers, exports, profile data, factory/seeder, listing/show/create/edit/print UI, i18n, and tests. The `nis_masters` settings module itself stays untouched. Followed by P6-T31 (certification document upload) as a separate task/branch.

### Boost MCP research (mandatory)
- `search-docs` queries=["drop column migration schema","spatie query-builder allowed filters exact","eloquent factory state"] → confirmed standard `dropColumn` migration syntax, factory `state()` convention; nothing contradicts existing code.
- Full-module inventory gathered by codebase exploration (see "Files to touch").
- Skills consulted: `laravel-best-practices`, `member-module-structure` (for cross-module references like team-quick-view, coach-picker), `pest-testing`, `validation-audit-guardrails`.

**Doc drift found:** None.

### Files to touch
- `database/migrations/2026_09_04_*_drop_nis_columns_from_coaches_table.php` — drop `nis_certified`, `nis_master_id` (+ FK)
- `app/Models/Coach.php` — remove fillable entries, boolean cast, `nisMaster()` relation, PHPDoc
- `app/Http/Requests/Coaches/StoreCoachRequest.php`, `UpdateCoachRequest.php` — remove `nis_certified`, `nis_master_id` rules
- `app/Http/Resources/CoachResource.php`, `app/Http/Resources/Api/V1/CoachSearchResource.php` — remove fields
- `app/Http/Controllers/CoachController.php` — remove `AllowedFilter::exact('nis_certified')`, `nisMasters` prop, NIS merge in `certificateTypes`, print column/rows
- `app/Http/Controllers/CoachExportController.php`, `app/Http/Controllers/TeamExportController.php` — remove column/eager-load
- `app/Support/Coaches/CoachProfileData.php` — remove eager load + detail payload
- `database/factories/CoachFactory.php`, `database/seeders/CoachSeeder.php` — remove `nis_certified`/`nis_master_id` + `nisCertified()` state
- `resources/js/pages/coaches/index.tsx` — remove column, filter select + param handling
- `resources/js/pages/coaches/show.tsx` — remove detail row/badge/getDetailValue
- `resources/js/pages/coaches/create.tsx`, `edit.tsx` — remove NIS info combobox, NIS certified checkbox, summary chip, types
- `resources/js/pages/coaches/print-preview.tsx` — remove column
- `resources/js/components/teams/team-quick-view.tsx`, `resources/js/components/coach-picker.tsx` — remove fields
- `resources/lang/hi.json` — remove NIS strings
- Tests: `CoachControllerTest`, `CoachExportControllerTest`, `TeamExportControllerTest`, `Api/V1/CoachSearchTest`, `CoachFactoryTest`, `CoachPreviewTest`, `CoachesMigrationTest`

### Schema / endpoints / pages
- Migration: drops two columns on `coaches`. No endpoint/page additions.

### Risk + mitigation
- **Risk:** hidden usages of the removed fields elsewhere (API consumers, reports).
  **Mitigation:** grep sweep across `app/`, `resources/js/`, `tests/`; full affected test files run green.
- **Risk:** `CoachSeeder` parses "NIS" from name strings to auto-flag certification — that seeding logic must be re-pointed or dropped along with the column.
  **Mitigation:** follow existing certification seeding pattern; keep seeder compiling.

### Test plan
- Pest Feature: update existing filter/CRUD/export/API tests to assert fields are gone (404/ignored payloads, no column in export rows); migration test asserts columns dropped
- Pest Unit: factory test without `nisCertified()` state
- Run: `php artisan test --compact` on affected files; `vendor/bin/pint --dirty`; `npm run type-check`

> User approved proposal 2026-09-04 ("okay"). Starting Gate 2.

---

## Summary (Gate 3) — fill after implementation

### What changed
- (pending)

### Decisions
- (pending)

### Tests added
- (pending)

### Deferred / follow-ups
- P6-T31 — Coach certification document upload + preview/download modal

### Definition of Done
- [ ] `php artisan migrate:fresh --seed` clean
- [ ] `php artisan migrate:rollback` clean on new migration
- [ ] Affected tests pass
- [ ] `vendor/bin/pint --test` passes
- [ ] `npm run type-check` passes

---

## Implementation notes (2026-09-04)

### What changed
- New migration `2026_09_04_150514_remove_nis_fields_from_coaches_table.php` drops `nis_certified` and `nis_master_id` (composite index `coaches_nis_masters_idx` dropped first, FK dropped only when it exists — MySQL/Postgres aware — and recreated on the remaining master columns). `down()` re-adds plain columns without the FK so full-batch rollbacks that later drop `nis_masters` keep working.
- `app/Models/Coach.php` — removed fillable entries, boolean cast, `nisMaster()` relation, PHPDoc props.
- `StoreCoachRequest` / `UpdateCoachRequest` — removed `nis_certified` and `nis_master_id` rules.
- `CoachResource`, `Api/V1/CoachSearchResource` — removed fields.
- `CoachController` — removed `AllowedFilter::exact('nis_certified')`, `nisMasters` prop (index/create/edit), NisMaster merge in `certificateTypes`, print column/label/cell/grids.
- `CoachExportController` — removed column from default columns/labels/filters, grouped listing export shrunk from 10 to 9 columns (lastColumn `I`, merge ranges, headings, widths).
- `TeamExportController` — dropped `nis_certified` from coach eager-load columns.
- `Api/V1/CoachSearchController`, `Api/V1/CoachPreviewController`, `Api/V1/TeamPreviewController` — removed field from selects/payloads.
- `CoachProfileData::shell()` — removed `nisMaster` eager load.
- `AuditLogBuilder` — removed `nis_certified` field label.
- `CoachFactory` — removed `nis_certified` default + `nisCertified()` state; `CoachSeeder` — removed "NIS" name parsing and nisCertified() fixtures.
- Backfill migration `2026_06_18_094000` — removed the `nis_certified` → "NIS" certification synthesis block (no-op on fresh DBs; already-run on existing ones).
- Frontend: `coaches/index.tsx` (type, filters, pill, table headers/cells, colgroups), `show.tsx`, `create.tsx`, `edit.tsx`, `print-preview.tsx`, `coach-picker.tsx`, `teams/team-quick-view.tsx`, `teams/coach-quick-view.tsx`; removed NIS strings from `resources/lang/hi.json`.
- Tests updated: `CoachControllerTest` (filter test now asserts 400 for removed filter; certificateTypes test asserts NisMaster names no longer merged), `CoachExportControllerTest` (headings/rows without NIS column), `TeamExportControllerTest`, `CoachSearchTest` (field absent from payload), `CoachPreviewTest`, `CoachFactoryTest` (state removed), `CoachesMigrationTest` (asserts columns dropped).

### Decisions
- `down()` re-adds `nis_master_id` as a plain nullable column (no FK) to keep `migrate:rollback` of a full batch working (nis_masters drop comes later in reverse order); mirrors the original create-migration fallback.
- Filtering by the removed `filter[nis_certified]` now returns 400 (Spatie invalid-filter); test asserts that contract.
- Pre-existing failures on HEAD (8 tests: print/export/team-export) are unrelated to this task and were left as-is; verified identical on base commit ae3f1e4 via `git stash`.

### Verification
- `php artisan migrate:fresh --seed` — clean (pgsql dev DB).
- `php artisan migrate:rollback` (full single batch) then `migrate` — clean both ways.
- Targeted Pest suite: 102 passed, 8 failed — the 8 failures match base-commit failures exactly.
- `vendor/bin/pint --dirty --format agent` — passed.
- `npm run types:check` — 0 errors in touched files; remaining errors are pre-existing in events/incharges/teams/tournaments show pages (untouched).

---

## Revision 1 (2026-09-04)

### Scope correction
- User rejected the removal of `nis_master_id`: only the `nis_certified` boolean is removed from the coach module. `nis_master_id` (FK to `nis_masters`, used for the "NIS info" certificate-type combobox) is restored across backend, frontend, and tests, matching pre-change state at `ae3f1e4`. The NIS masters settings module (`NisMaster` model, settings controller/policy, `resources/js/pages/settings/nis-masters`) was never touched and stays as-is.

### What changed since the original commit
- Migration `2026_09_04_150514_remove_nis_fields_from_coaches_table.php` now drops only `nis_certified` (guarded by `Schema::hasColumn`); no index/FK handling, no DB import. `down()` re-adds only `nis_certified`.
- `app/Models/Coach.php` — restored `@property int|null $nis_master_id`, fillable entry, and the `nisMaster()` BelongsTo relation. `nis_certified` cast/property stay removed.
- `StoreCoachRequest` / `UpdateCoachRequest` — restored `nis_master_id` rule (`exists:nis_masters,id` with `kind = nis`; nullable / sometimes+nullable).
- `CoachResource` — restored `nis_master_id` field.
- `CoachController` — restored `NisMaster` import, `nisMasters` prop in index/create/edit, and the NisMaster name merge in `certificateTypes`. `AllowedFilter::exact('nis_certified')` and the print NIS column/label/cells stay removed.
- `CoachProfileData::shell()` — restored `nisMaster:id,kind,code,name,short_name` eager load.
- `resources/js/pages/coaches/create.tsx` / `edit.tsx` — restored `NisMasterOption` type, `nisMasters` prop, `nis_master_id` form field (string) with defaults, `nisItems` combobox items, and the "NIS info" Combobox UI next to Tier / level (tier + NIS share one `sm:grid-cols-2` grid, email starts the next — matching pre-change layout).
- Tests: `CoachesMigrationTest` now asserts `nis_certified` dropped / `nis_master_id` kept; restored `CoachControllerTest` case "index includes active nis master names as certificate type filter values" (asserts `certificateTypes.0` = 'NIS diploma').
- Kept removed (verified no `nis_master_id` present pre-change, so nothing to restore): `CoachExportController`, `TeamExportController`, `Api/V1/CoachSearchController`, `Api/V1/CoachPreviewController`, `Api/V1/TeamPreviewController`, `CoachFactory`, `CoachSeeder`, `AuditLogBuilder` label, backfill migration NIS-synthesis block, and frontend `index.tsx` / `show.tsx` / `print-preview.tsx` / quick-view components / `hi.json` NIS strings.

### Verification
- `php artisan migrate:fresh --seed` — clean; `migrate:rollback` then `migrate` — clean both ways; tinker check confirms `coaches.nis_certified` gone, `coaches.nis_master_id` present (pgsql dev DB).
- Grep: zero `nis_certified` hits in `app/` and `resources/js/`; `nis_master_id` present in model, both requests, `CoachResource`, `CoachController` (3 props + certificateTypes concat), create/edit tsx.
- Targeted Pest suite: 102 passed, 8 failed — the 8 failures (print/export/team-export) are pre-existing and identical on base commit `ae3f1e4`; the restored certificateTypes test passes.
- `vendor/bin/pint --dirty --format agent` — passed.
- `npm run types:check` — 0 errors in touched files; remaining errors are pre-existing in events/incharges/teams/tournaments show pages (untouched).

---

## Revision 2 (2026-09-04)

### Requested by user
"Display NIS info on show page" — the selected NIS certificate is set via the create/edit combobox but was never visible on the profile page.

### What changed
- `app/Http/Resources/CoachResource.php` — added `nis_master` whenLoaded payload (id, code, name, short_name), mirroring `rank_master` / `designation_master`.
- `resources/js/pages/coaches/show.tsx` — `nis_master` type on Coach + "NIS info" row in the overview details grid (after Designation).
- `tests/Feature/CoachControllerTest.php` — new test: show includes nis master name when coach has nis info.

### Why
`nis_master_id` is editable but had no read-only surface; `CoachProfileData::shell()` already eager-loads the relation.

### Tests updated
Yes — new Inertia assertion test passes; pint + types:check clean for touched files.
