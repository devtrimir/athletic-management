# P5-T09 — API: member participations + achievements endpoints

- **Task ID:** P5-T09
- **Branch:** `feat/p5-t09-api-member-participations-achievements`
- **Started:** 2026-05-24
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
Add `GET /api/v1/members/{member}/participations` (grouped by session) and `GET /api/v1/members/{member}/achievements` (summary + list). From TASKS.md: "API: members participations + achievements endpoints".

### Boost MCP research (mandatory)
- `search-docs` queries=["API resources collection", "groupBy collection"] → use `->groupBy()` on Eloquent collection; inline array mapping in controllers (same pattern as ReferenceDataController for bespoke shapes)
- Existing pattern: `MemberTeamsController` — invokable, `Gate::authorize('view', $member)`, `response()->json()` or `Resource::collection()->response()`
- `BelongsToOrganization` global scope on Member model provides automatic cross-org 404 on route binding

**Doc drift found:** None.

### Files to touch
- `app/Http/Controllers/Api/V1/MemberParticipationsController.php` — create
- `app/Http/Controllers/Api/V1/MemberAchievementsController.php` — create
- `routes/api.php` — add 2 GET routes
- `tests/Feature/MemberParticipationsAchievementsApiTest.php` — create

### Endpoints
- `GET /api/v1/members/{member}/participations` — chronological grouped by session
- `GET /api/v1/members/{member}/achievements` — summary counts + list

### Test plan
- Participations: unauthenticated → 401; no members.view → 403; shape correct; empty returns []
- Achievements: unauthenticated → 401; no members.view → 403; summary counts match; cross-org member → 404

> **User approved Gate 1 — proceeding to Gate 2.**

---

## Summary (Gate 3) — fill after implementation
