# P1-T01 — Migration: organizations

> **Retroactive backfill** — journal was not created at task start. Filled from git history and conversation records on 2026-05-21.

- **Task ID:** P1-T01
- **Phase file:** [`docs/phases/P01-foundation.md`](../P01-foundation.md)
- **Branch:** `feat/p1-t01-organizations-migration` (commits merged directly to `main`)
- **Started:** 2026-05-21
- **Status:** merged

---

## Plan (Gate 1)

### Scope
Create the `organizations` table — the root tenant anchor for the entire system. Add `Organization` Eloquent model, factory, and Pest feature test. Every domain table will later reference `organizations.id`.

### Boost MCP research
- `search-docs` packages=[laravel/framework] queries=[migrations foreignId, eloquent model factory, pest feature test RefreshDatabase] → confirmed `foreignId()->constrained()->cascadeOnDelete()` pattern and PHP attribute `#[Fillable]` usage.
- Skills consulted: `laravel-best-practices`, `pest-testing`

**Doc drift found:** None.

### Files to touch
- `database/migrations/2026_05_21_113521_create_organizations_table.php` — new
- `app/Models/Organization.php` — new
- `database/factories/OrganizationFactory.php` — new
- `tests/Feature/OrganizationsMigrationTest.php` — new

### Schema / endpoints / pages
- Migration: `organizations` — `id`, `name`, `code` (unique), `is_active`, `created_at`, `updated_at`

### Risk + mitigation
- **Risk:** All later FK migrations depend on this table existing first.
  **Mitigation:** Task is first in the P1 schema sequence; no downstream tables created in this task.

### Test plan
- Pest Feature: columns present, factory creates valid record, `code` unique constraint enforced

---

## Summary (Gate 3)

### What changed
- `database/migrations/2026_05_21_113521_create_organizations_table.php` — `organizations` table: `id`, `name` string, `code` string unique, `is_active` boolean default true, timestamps
- `app/Models/Organization.php` — `#[Fillable(['name','code','is_active'])]`, `HasFactory`
- `database/factories/OrganizationFactory.php` — `name` via `fake()->company()`, `code` via `fake()->unique()->lexify('???')` uppercased, `is_active` true
- `tests/Feature/OrganizationsMigrationTest.php` — 3 tests: columns exist, factory creates record, unique constraint

### Decisions
- **`code` unique:** Organizations are referenced by short code (e.g. `UPP`) in seeds and fixtures — unique constraint enforced at DB level, not only validation.
- **`#[Fillable]` attribute:** Project convention — PHP 8 attribute replaces `protected $fillable` array.

### Tests added
- `tests/Feature/OrganizationsMigrationTest.php` — columns, factory, uniqueness

### Deferred / follow-ups
- Seeder for default `UPP` org deferred to P1-T19.

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan migrate:rollback` clean on new migrations
- [x] Feature test per new endpoint
- [ ] Unit test per new Service/Action — N/A
- [ ] Vitest test per non-trivial component — N/A
- [ ] Pest Browser happy-path — N/A
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [ ] `vendor/bin/phpstan analyse` — not run this task
- [ ] `npm run lint` — N/A
- [ ] `npm run type-check` — N/A
- [ ] `npm run build` — N/A
- [ ] Authorization gate / Policy — N/A (schema-only task)
- [ ] Audit log fires — N/A
- [ ] No new hard-coded user-facing strings — N/A
- [x] Phase file route/page table — N/A (no routes)
- [ ] ADR filed — N/A

---

## Approval (Gate 5)

## Approved — 2026-05-21 by raghvendrasingh
**Squash commit:** `feat(p1): add organizations migration, model, and factory`
**Merged:** 2026-05-21
