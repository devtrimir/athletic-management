# P0-T07 — `/api/health` endpoint

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only.

- **Task ID:** P0-T07
- **Phase file:** [`docs/phases/P00-bootstrap.md`](../P00-bootstrap.md)
- **Branch:** `feat/p0-t07-health-endpoint`
- **Started:** 2026-05-21
- **Status:** awaiting-approval

---

## Plan (Gate 1)

### Scope
Add `GET /api/health` returning `{"status":"ok"}` (stateless, no auth) and a Pest Feature test asserting 200 + exact JSON shape.

### Boost MCP research (mandatory)
- `search-docs` packages=[laravel/framework] queries=["api routing", "routes api.php registration", "withRouting api"]
  → `php artisan install:api` creates `routes/api.php` but also installs Sanctum (forbidden). Must manually create `routes/api.php` and register via `withRouting(api:)`. `/api` prefix + `api` middleware group applied automatically.
- Skills consulted: `pest-testing`

**Doc drift found:** None.

### Files to touch
- `routes/api.php` — create with `GET /health`
- `bootstrap/app.php` — add `api:` key to `withRouting()`
- `tests/Feature/HealthEndpointTest.php` — Pest Feature test
- `phpstan-baseline.neon` — regenerated to include `getJson()` false-positive (same Pest `$this`-binding limitation already baselined for `get()`, `post()`, `actingAs()` etc.)

### Endpoints
- `GET /api/health` → `200 {"status":"ok"}`, no auth, stateless

### Risk + mitigation
- **Risk:** `bootstrap/app.php` already has `health: '/up'` — Laravel's built-in health check. No conflict; `/api/health` is a separate project endpoint.
- **Risk:** `php artisan install:api` installs Sanctum (forbidden). Mitigation: manual route file + `withRouting(api:)`.

### Test plan
- Pest Feature `HealthEndpointTest` — `GET /api/health` returns 200 + `{"status":"ok"}`

---

## Summary (Gate 3)

### What changed
- `routes/api.php` — created; single `GET /health` closure route
- `bootstrap/app.php` — added `api: __DIR__.'/../routes/api.php'` to `withRouting()`
- `tests/Feature/HealthEndpointTest.php` — Pest Feature test; `assertSuccessful()` + `assertExactJson(['status' => 'ok'])`
- `phpstan-baseline.neon` — regenerated; adds `getJson()` to the existing list of Pest `$this`-binding false-positives

### Decisions
- **No `install:api`:** would install Sanctum — forbidden. Manual approach is two lines and zero deps.
- **`assertExactJson`** over `assertJson`: enforces exact shape with no extra keys.

### Tests added
- `tests/Feature/HealthEndpointTest.php` — covers `GET /api/health` → 200 + exact JSON

### Deferred / follow-ups
- None

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean (no migrations in this task)
- [x] Feature test per new endpoint
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [x] `vendor/bin/phpstan analyse` passes
