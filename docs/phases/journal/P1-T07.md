# P1-T07 — Migration: units

> **Retroactive backfill** — journal was not created at task start. Filled from git history and conversation records on 2026-05-21.

- **Task ID:** P1-T07
- **Phase file:** [`docs/phases/P01-foundation.md`](../P01-foundation.md)
- **Branch:** `feat/p1-t07-units` (commits merged directly to `main`)
- **Started:** 2026-05-21
- **Status:** merged

---

## Plan (Gate 1)

### Scope
Create the `units` table (police units / battalions that own athlete rosters). Org-scoped. Model, factory, Pest feature test.

### Boost MCP research
- `search-docs` packages=[laravel/framework] queries=[migration foreignId cascadeOnDelete, org-scoped table] → standard pattern confirmed.
- Skills consulted: `laravel-best-practices`, `pest-testing`

**Doc drift found:** None.

### Files to touch
- `database/migrations/2026_05_21_121002_create_units_table.php` — new
- `app/Models/Unit.php` — new
- `database/factories/UnitFactory.php` — new
- `tests/Feature/UnitsMigrationTest.php` — new

### Schema / endpoints / pages
- Migration: `units` — `id`, `organization_id` FK (cascadeOnDelete), `name_hi`, `name_en`, `code` string, `is_active` boolean, timestamps; unique `(organization_id, code)`

### Risk + mitigation
- **Risk:** None significant.

### Test plan
- Pest Feature: columns, FK, factory, unique `(organization_id, code)` constraint

---

## Summary (Gate 3)

### What changed
- `database/migrations/2026_05_21_121002_create_units_table.php` — `units`: org-scoped (cascadeOnDelete), `name_hi`, `name_en`, `code`, `is_active`, timestamps; unique `(organization_id, code)`
- `app/Models/Unit.php` — `#[Fillable(['organization_id','name_hi','name_en','code','is_active'])]`, `HasFactory`
- `database/factories/UnitFactory.php` — generates battalion-style names
- `tests/Feature/UnitsMigrationTest.php` — columns, FK, factory, uniqueness

### Decisions
- **`organization_id` cascadeOnDelete:** Units belong strictly to an org — if the org is deleted, all its units should be removed too.

### Tests added
- `tests/Feature/UnitsMigrationTest.php`

### Deferred / follow-ups
- CRUD UI → P1-T27
- Seeder for initial units: no dedicated seeder planned; admin creates via UI.

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan migrate:rollback` clean
- [x] Feature tests pass
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes

---

## Approval (Gate 5)

## Approved — 2026-05-21 by raghvendrasingh
**Squash commit:** `feat(p1): units migration, model, and factory (P1-T07)`
**Merged:** 2026-05-21
