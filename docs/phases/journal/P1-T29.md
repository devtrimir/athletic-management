# P1-T29 — TournamentTiers CRUD

- **Task ID:** P1-T29
- **Phase file:** [`docs/phases/P01-reference-data.md`](../P01-reference-data.md)
- **Branch:** `feat/p1-t29-tournament-tiers-crud`
- **Started:** 2025-07-23
- **Status:** merged

---

## Plan (Gate 1)

### Scope
TournamentTiers CRUD slice under Settings. Global reference table (no organization_id), enum code with 6 fixed values, weight (smallInteger) for ordering. Fields: code (enum), label_hi, label_en, weight.

### Boost MCP research (mandatory)
- `database-schema` table=`tournament_tiers` → id, code ENUM unique, label_hi, label_en, weight smallInteger, timestamps — NO organization_id
- Factory has exactly 6 entries (one per enum value) using fake()->unique()

**Doc drift found:** None

### Files to touch
- `app/Http/Requests/Settings/StoreTournamentTierRequest.php` — new
- `app/Http/Requests/Settings/UpdateTournamentTierRequest.php` — new
- `app/Policies/TournamentTierPolicy.php` — new (no org-scope)
- `app/Http/Controllers/Settings/TournamentTierController.php` — new
- `routes/settings.php` — add TournamentTierController + resource route
- `resources/js/pages/settings/tournament-tiers/index.tsx` — new
- `resources/js/pages/settings/tournament-tiers/create.tsx` — new (code uses Select dropdown)
- `resources/js/pages/settings/tournament-tiers/edit.tsx` — new
- `tests/Feature/Settings/TournamentTierCrudTest.php` — new

### Risk + mitigation
- **Risk:** factory only has 6 entries — test exhaustion with fake()->unique()
  **Mitigation:** use explicit code values in tests (max 1 per code per test)

---

## Summary (Gate 3)

### What changed
- `StoreTournamentTierRequest` — required fields; code validated with `Rule::in([...])` + globally unique
- `UpdateTournamentTierRequest` — same with `->ignore($tierId)` on code
- `TournamentTierPolicy` — all methods check `reference_data.manage` only; no org-scope
- `TournamentTierController` — CRUD, ordered by weight desc, no org filter
- `routes/settings.php` — `Route::resource('settings/tournament-tiers', ...)->names('tournament-tiers')`
- React pages — index (code/label_hi/label_en/weight columns), create/edit (Select for code, number input for weight)
- Pest Feature test — 17 tests, all pass

### Decisions
- **Select for code field:** enum constraint means free-text input is wrong; Select dropdown shows 6 fixed options.
- **Ordered by weight desc:** higher weight = more prestigious tier, shown first.
