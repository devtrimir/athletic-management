# P2-T18 — Pages/Members/Edit.tsx

- **Task ID:** P2-T18
- **Phase file:** [`docs/phases/P02-members.md`](../P02-members.md)
- **Branch:** `feat/p2-t18-member-edit`
- **Started:** 2026-05-23
- **Status:** merged

---

## Plan (Gate 1)

### Scope
`Pages/Members/Edit.tsx` — pre-filled `useForm` edit form. Same 13 fields as Create, initialised from the `member` Inertia prop. Submits `PATCH` via Wayfinder `update.patch(member.id)`.

### Boost MCP research (mandatory)
- Pattern identical to T17; no new docs queries needed — same packages, same Inertia useForm approach
- Skills consulted: `inertia-react-development`, `wayfinder-development`

**Doc drift found:** None.

### Files to touch
- `resources/js/pages/members/edit.tsx` — replace stub with pre-filled useForm edit form
- `resources/lang/hi.json` — 2 new strings: `Edit member`, `Save changes`

### Schema / endpoints / pages
- Page: `resources/js/pages/members/edit.tsx` — Edit member → `PATCH /members/{member}` via `update.url(member)`

### Risk + mitigation
- **Risk:** FK fields arrive as int|null from raw model; Select needs string
  **Mitigation:** `member.home_district_id != null ? String(...) : ''`

### Test plan
- Deferred to T22–T26

---

## Summary (Gate 3)

### What changed
- `resources/js/pages/members/edit.tsx` — Full `useForm` edit form: 3 card sections, all 13 fields pre-filled from `member` prop, `patch()` submit, cancel → `show.url(member)`
- `resources/lang/hi.json` — 2 new strings (Edit member, Save changes)

### Decisions
- **`patch()` over `put()`**: `UpdateMemberRequest` uses `sometimes` (partial update); PATCH is semantically correct
- **FK init guard**: `!= null` check avoids `"null"` string in Select value
- **Cancel → `show.url(member)`**: returns to profile, not the list

### Tests added
- None (deferred to T22–T26)

### Definition of Done
- [x] Code compiles / type-checks (`npm run build` → ✓ 2.84s, no errors)
- [x] All strings via `t()` — 2 new entries added to `hi.json`
