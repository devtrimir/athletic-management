# P2C-T01 — Member Sport Filter (FK + Filter Pill + Form Select)

- **Task ID:** P2C-T01
- **Branch:** `feat/p2c-t01-member-sport-filter`
- **Started:** 2026-05-29
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
Add a nullable `sport_id` FK to `members`, expose it as a `<Select>` in Create/Edit forms, add a Sport `FilterPill` on the members listing, and wire `AllowedFilter::exact('sport_id')` in `MemberController::index()`.

### Files to touch
- `database/migrations/…_add_sport_id_to_members_table.php` — nullable FK + index
- `app/Models/Member.php` — `sport()` BelongsTo + `sport_id` fillable + docblock
- `app/Http/Resources/MemberResource.php` — expose `sport` whenLoaded
- `app/Http/Requests/Members/StoreMemberRequest.php` — `sport_id` nullable exists rule
- `app/Http/Requests/Members/UpdateMemberRequest.php` — same, `sometimes`
- `app/Http/Controllers/MemberController.php` — filter + `sports` prop to index/create/edit
- `resources/js/pages/members/index.tsx` — `sport_id` in Filters + Sport FilterPill
- `resources/js/pages/members/create.tsx` — Sport select dropdown
- `resources/js/pages/members/edit.tsx` — Sport select dropdown
- `resources/lang/hi.json` — "Sport", "Search sports…"

### Schema / endpoints / pages
- Migration: `add_sport_id_to_members_table` — nullable `sport_id` FK to `sports`, index, reversible down

### Risk + mitigation
- **Risk:** existing members have null sport_id — filter returns subset only when selected
  **Mitigation:** filter is nullable/optional, null sport_id members still appear when filter not set

### Test plan
- Pest Feature: `filter[sport_id]` returns only members of that sport
- Pest Feature: `sport_id` validated (must exist in sports, nullable)

---

## Summary (Gate 3)

*Filled after implementation.*
