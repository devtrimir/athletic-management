# P6-T53 — Fix missing translations and localize enum labels on team prabhari (incharge) pages

- **Task ID:** P6-T53
- **Phase file:** `docs/TASKS.md` (Phase 6 follow-up)
- **Branch:** `fix/p6-t53-incharge-translations`
- **Started:** 2026-09-05
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
The team prabhari (incharge) module currently shows several English labels when the application language is set to Hindi. This task audits all translation keys used in the incharge pages/components and adds the missing Hindi and English keys. It also localizes raw enum codes (achievement level, medal type, gender class) that are displayed without translation.

### Files to touch
- `resources/js/pages/incharges/index.tsx` — breadcrumb translation.
- `resources/js/pages/incharges/show.tsx` — localized enum display for level/medal/gender, localized select labels.
- `resources/js/pages/incharges/print.tsx` — localized print heading and enum normalization.
- `resources/js/components/teams/team-incharge-panel.tsx` — ensure labels are keyed (already mostly wrapped; add missing English keys only).
- `resources/lang/en.json` — add missing English keys used by incharge module.
- `resources/lang/hi.json` — add missing Hindi keys used by incharge module.

### Schema / endpoints / pages
- No migrations or backend changes.
- Pages affected:
  - `/incharges`
  - `/incharges/{incharge}`
  - `/incharges/{incharge}/preview`
  - `/teams/{team}` (team prabhari panel)

### Risk + mitigation
- **Risk:** Adding duplicate keys or changing key casing breaks existing translations.
  **Mitigation:** Only add keys that do not already exist; keep existing keys untouched.
- **Risk:** Localizing enums changes display in English if keys are missing.
  **Mitigation:** Add uppercase enum keys to `en.json` as well.

### Test plan
- Manual UI check: switch app language to Hindi and open incharge listing/show/preview and team prabhari panel; verify labels appear in Hindi.
- Run `npm run build` and `npx eslint` on changed files.
- Run `php artisan test --compact` (no PHP changes, but verify no regressions).

> **Approved by user:** (pending)

---

## Summary (Gate 3)

### Implemented
- Audited all `t(...)` keys used in `resources/js/pages/incharges/*` and `resources/js/components/teams/team-incharge-panel.tsx`.
- Added 90 missing English keys to `resources/lang/en.json` and 28 missing Hindi keys to `resources/lang/hi.json`.
- Localized enum labels in the incharge module:
  - Achievement level, medal type, and gender class in `show.tsx`.
  - Achievement level, medal type, sport/discipline, and special-achievement type in `print.tsx`.
- Replaced the hard-coded incharge print letterhead with the existing translatable key.
- Updated the shared `<Breadcrumbs>` component to translate breadcrumb titles, which also fixes breadcrumbs for the incharge (and other) modules when the app language is Hindi.

### Verification
- `npx eslint resources/js/pages/incharges/show.tsx resources/js/pages/incharges/print.tsx resources/js/components/breadcrumbs.tsx` — clean.
- `npm run build` — succeeded.
- `vendor/bin/pest tests/Feature/PromotionDependencyGuardTest.php --compact` — 7 passed (full suite currently fails on a pre-existing duplicate helper function in `MemberExportControllerTest.php` / `MemberPostingAndRankTest.php`).

### Pull Request
- **PR:** #210 — https://github.com/devtrimir/athletic-management/pull/210

---

## Revisions (Gate 4)

<!-- Append only if user requests changes -->

---

## Approval (Gate 5)

<!--
## Approved — YYYY-MM-DD by <user-handle>
**PR:** #<number> — <link>
**Squash commit:** `<type>(<scope>): <subject>`
**Merged:** YYYY-MM-DD
-->
