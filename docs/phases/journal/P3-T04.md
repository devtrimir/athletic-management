# P3-T04 — Form Requests + CoachController (Inertia CRUD)

- **Task ID:** P3-T04
- **Phase file:** [`docs/phases/P03-coaches.md`](../P03-coaches.md)
- **Branch:** `feat/p3-t04-coach-controller`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
`CoachController` with 7 Inertia CRUD actions, `StoreCoachRequest`, `UpdateCoachRequest`, `CoachResource`, and resource route.

### Boost MCP research (mandatory)
- `search-docs` packages=[inertiajs/inertia-laravel] queries=["deferred props", "flash"] → confirmed `Inertia::defer()` and `Inertia::flash()` patterns
- `database-schema` table=`coaches` → confirmed fillable fields
- Skills consulted: laravel-best-practices SKILL.md, inertia-react-development SKILL.md

**Doc drift found:** None.

### Files to touch
- `app/Http/Requests/Coaches/StoreCoachRequest.php` — new
- `app/Http/Requests/Coaches/UpdateCoachRequest.php` — new
- `app/Http/Resources/CoachResource.php` — new
- `app/Http/Controllers/CoachController.php` — new
- `routes/web.php` — add `Route::resource('coaches', CoachController::class)`
- `docs/TASKS.md` — tick P3-T04

### Schema / endpoints / pages
- `GET /coaches` → coaches/index (QueryBuilder filters, paginate 25)
- `GET /coaches/create` → coaches/create
- `POST /coaches` → store → redirect show
- `GET /coaches/{coach}` → coaches/show (Deferred member)
- `GET /coaches/{coach}/edit` → coaches/edit
- `PATCH /coaches/{coach}` → update → redirect show
- `DELETE /coaches/{coach}` → soft-delete → redirect index

### Test plan
- Deferred to P3-T09/T10 (after pages exist)

---

## Summary (Gate 3)

### What changed
- `app/Http/Requests/Coaches/StoreCoachRequest.php` — validates `full_name_hi`, `full_name_en`, `pno` unique per org, `mobile`, `nis_certified`, `member_id` exists in org
- `app/Http/Requests/Coaches/UpdateCoachRequest.php` — `sometimes` rules, unique ignores current coach
- `app/Http/Resources/CoachResource.php` — id, names, pno, mobile, nis_certified, `whenLoaded('member')`
- `app/Http/Controllers/CoachController.php` — 7 actions; `Inertia::defer` for member on show
- `routes/web.php` — resource route added; Wayfinder regenerated

### Decisions
- **Tests deferred:** Inertia page components don't exist yet; full tests done in T09/T10

### Deferred / follow-ups
- Tests → P3-T09, P3-T10

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [x] Authorization gate / Policy added or updated

---

## Approval (Gate 5)

## Approved — 2026-05-24
**PR:** #75 — feat/p3-t04-coach-controller
**Squash commit:** `feat(coaches): CoachController, Form Requests, CoachResource, routes`
**Merged:** 2026-05-24
