# P6-T32 — Remove coach designation fields + show NIS info on active listing

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only: never delete old sections, never rewrite history.

- **Task ID:** P6-T32
- **Phase file:** P6
- **Branch:** `feat/p6-t32-remove-coach-designation`
- **Started:** 2026-09-04
- **Status:** planning

---

## Plan (Gate 1)

### Scope
Remove BOTH coach designation fields — the legacy free-text `designation` column and the `designation_master_id` combobox (designations master) — from the coach module end-to-end: DB columns dropped, model, form requests, controller props, print, exports, API search/preview, profile resource/data, create/edit/show/print-preview/quick-view/coach-picker UI, and tests. The designations master settings module itself stays (other modules use it). Separately, add an "NIS info" column to the **active** coaches listing showing the resolved `nis_master` name (new eager-load in `listingQuery`).

User decisions 2026-09-04: remove both designation fields; drop the DB columns (confirmed explicitly after the P6-T30 over-removal lesson).

### Boost MCP research (mandatory)
- `search-docs` queries=["drop foreign key column migration","eager load nested relationship query"] → standard dropColumn/dropForeign + `with()` eager-load confirmed; nothing contradicts existing code. Pattern precedent: P6-T30 migration `2026_09_04_150514_remove_nis_fields_from_coaches_table.php` (driver-aware FK guard).
- Full usage inventory gathered by codebase exploration.
- Skills consulted: `laravel-best-practices`, `pest-testing`, `validation-audit-guardrails`, `member-module-structure`.

**Doc drift found:** None.

### Files to touch
- `database/migrations/2026_09_04_*_remove_designation_fields_from_coaches_table.php` — drop `designation`, `designation_master_id` (FK/index driver-aware, mirror P6-T30 migration)
- `app/Models/Coach.php` — fillable/PHPDoc/`designationMaster()` relation
- `app/Http/Requests/Coaches/StoreCoachRequest.php` / `UpdateCoachRequest.php` — both field rules
- `app/Http/Controllers/CoachController.php` — `$payload['designation'] ?? null`, `designations` prop (index/create/edit), print label + `printColumnValue` 'coach' prepend, `allowedSorts` 'designation'
- `app/Http/Controllers/CoachExportController.php` — label, 'coach' column prepend, allowedSorts, single-coach eager-load
- `app/Http/Resources/CoachResource.php` — designation fields + `designation_master` block
- `app/Support/Coaches/CoachProfileData.php` — `designationMaster` eager load in shell()
- `app/Http/Resources/Api/V1/CoachSearchResource.php`, `app/Http/Controllers/Api/V1/CoachSearchController.php` (selects + fuzzy orWhere), `app/Http/Controllers/Api/V1/CoachPreviewController.php`
- `app/Services/AuditLogBuilder.php:550` — coach changelog label
- `database/migrations/2026_06_18_094000_backfill_*.php` — designation backfill block
- `database/factories/CoachFactory.php`
- `resources/js/pages/coaches/create.tsx` / `edit.tsx` — Designation combobox + types + form state + subtitle + errorKeys
- `resources/js/pages/coaches/show.tsx` — type, header badge, overview row
- `resources/js/pages/coaches/print-preview.tsx` — type, subtitle, overview row
- `resources/js/components/coach-picker.tsx`, `resources/js/components/teams/coach-quick-view.tsx`
- `resources/js/pages/coaches/index.tsx` — vestigial `designation` in Coach/Filters types + activeFilterCount
- `app/Http/Controllers/CoachController.php::listingQuery()` — add `nisMaster:id,kind,code,name,short_name` eager-load
- `resources/js/pages/coaches/index.tsx` — active-tab table: `nis_master` type, colgroup rebalance (6→7 cols), header cell, row cell, `buildCoachTeamSportRows` population
- Tests: `CoachControllerTest`, `CoachExportControllerTest`, `Api/V1/CoachSearchTest`, `CoachPreviewTest`, `CoachesMigrationTest` + new listing assertion for nis_master

### Schema / endpoints / pages
- Migration: drop 2 columns + FK/index on `coaches`
- No endpoint changes; page change: coaches index active tab gains a column

### Risk + mitigation
- **Risk:** hi.json "Designation"/"Select designation" keys are shared with member/incharge modules — deleting them breaks other pages.
  **Mitigation:** remove only coach-scoped strings; verify key usage with grep before deleting.
- **Risk:** `table-fixed` colgroup on active listing needs width rebalance for a 7th column.
  **Mitigation:** adjust all percentage widths in both colgroup blocks (header + body), visual check via screenshot if feasible.

### Test plan
- Pest Feature: update affected tests (prop absence, sort/filter removal, export/print rows, migration asserts columns dropped); new: index payload includes `coaches.data.*.nis_master.name` for active listing
- Run: `php artisan test --compact` on affected files; `vendor/bin/pint --dirty`; `npm run types:check` on touched files

> User approved scope 2026-09-04. Starting Gate 2.

---

## Summary (Gate 3) — fill after implementation

### What changed
- (pending)

### Decisions
- (pending)

### Tests added
- (pending)

### Deferred / follow-ups
- (pending)

### Definition of Done
- [ ] `php artisan migrate:fresh --seed` clean
- [ ] `php artisan migrate:rollback` clean on new migration
- [ ] Affected tests pass
- [ ] `vendor/bin/pint --test` passes
- [ ] `npm run types:check` passes on touched files

---

## Implementation notes (2026-09-04)

### What changed
- New migration `2026_09_04_160000_remove_designation_fields_from_coaches_table.php` drops `designation`, `designation_master_id`, and `designation_en` (driver-aware FK/composite-index handling via `Schema::getIndexes()` — works on MySQL + SQLite; `down()` re-adds plain nullable columns + restores index).
- Backend: Coach model (fillable/PHPDoc/relation), both form requests, CoachController (payload line, 3× `designations` props, print label/coach-column prepend, allowedSorts), CoachExportController (labels/sorts/coach column), CoachResource, CoachProfileData shell eager-load, CoachSearchResource/Controller (selects + fuzzy orWhere), CoachPreviewController, backfill migration, CoachFactory.
- Frontend: create/edit (types, form state, subtitle, errorKeys, combobox + items builder — NIS info combobox moved into the vacated grid slot next to Rank), show (badge, overview row, ALL_COLUMNS key), print-preview, coach-picker, coach-quick-view, index (type + vestigial filter refs).
- Active listing: `listingQuery()` eager-loads `nisMaster`; index.tsx gained `nis_master` type, `TeamCoach.nis_master_name`, and an "NIS info" column after Posting (colgroup rebalanced: Rank 10 / Name 22 / PNO 13 / Mobile 11 / Role 10 / Posting 12 / NIS 12). Inactive tab untouched.
- Tests: `CoachesMigrationTest` asserts all 3 columns dropped; `CoachControllerTest` new test asserting resolved `nis_master.name` on index payload rows.

### Decisions
- **`designation_en` dropped too** — coaches-table column placed after `designation`, zero reads anywhere; incharges-side column untouched.
- **`AuditLogBuilder` unchanged** — its 'designation' label is Incharge-context; Coach label map never had it.
- **hi.json untouched** — "Designation"/"Select designation" still used by incharges pages; "NIS info" falls back to English like existing NIS strings.
- Pre-existing failures (4 print + 2 export + "index defaults to active" on main) left as-is; verified identical via stash.

### Verification
- `migrate:fresh --seed` clean; rollback + migrate clean.
- Five targeted test files: 94 passed / 7 failed — failures identical to `main`.
- Pint passed; `npm run types:check` clean on touched files; prettier clean.

---

## Approval (Gate 5)

## Approved — 2026-09-04 by user
**PR:** #183 — https://github.com/devtrimir/athletic-management/pull/183
**Merge commit:** `539497e`
**Merged:** 2026-09-04
**Post-merge verification:** 88 passed / 5 failed on main — failures are the known pre-existing print/index ones; branch deleted locally and remotely.

### DB-handling note (user directive, applies to all future tasks)
User instructed: **never run `migrate:fresh` / rollbacks against the dev DB (`uppscb1`) — treat it like a live application.** All migration verification must happen on a scratch database. Journal `_TEMPLATE.md` DoD updated accordingly (commit `19b6771`).
