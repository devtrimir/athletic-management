# P1-T06 — Migration: sports

> **Retroactive backfill** — journal was not created at task start. Filled from git history and conversation records on 2026-05-21.

- **Task ID:** P1-T06
- **Phase file:** [`docs/phases/P01-foundation.md`](../P01-foundation.md)
- **Branch:** `feat/p1-t06-sports` (commits merged directly to `main`)
- **Started:** 2026-05-21
- **Status:** merged

---

## Plan (Gate 1)

### Scope
Create the `sports` global reference table. Model, factory, and Pest feature test. No `organization_id` — sports are a global catalog, not tenant-scoped.

### Boost MCP research
- `search-docs` packages=[laravel/framework] queries=[migration global reference table, factory unique sequence] → standard pattern confirmed.
- Skills consulted: `laravel-best-practices`, `pest-testing`

**Doc drift found:** None.

### Files to touch
- `database/migrations/2026_05_21_120539_create_sports_table.php` — new
- `app/Models/Sport.php` — new
- `database/factories/SportFactory.php` — new
- `tests/Feature/SportsMigrationTest.php` — new

### Schema / endpoints / pages
- Migration: `sports` — `id`, `name_hi` string, `name_en` string, `code` string unique, `is_active` boolean default true, timestamps. No `organization_id`.

### Risk + mitigation
- **Risk:** None significant for a reference table.

### Test plan
- Pest Feature: columns, factory, unique code constraint

---

## Summary (Gate 3)

### What changed
- `database/migrations/2026_05_21_120539_create_sports_table.php` — `sports`: `name_hi`, `name_en`, `code` unique, `is_active`, timestamps; no org scope
- `app/Models/Sport.php` — `#[Fillable(['name_hi','name_en','code','is_active'])]`, `HasFactory`
- `database/factories/SportFactory.php` — bilingual names from faker; unique code
- `tests/Feature/SportsMigrationTest.php` — columns, factory, unique constraint

### Decisions
- **No `organization_id`:** Sports catalog is global — same sports are available to all organizations.

### Tests added
- `tests/Feature/SportsMigrationTest.php`

### Deferred / follow-ups
- Seeder for ~30 sports → P1-T22
- CRUD UI → P1-T26

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan migrate:rollback` clean
- [x] Feature tests pass
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes

---

## Approval (Gate 5)

## Approved — 2026-05-21 by raghvendrasingh
**Squash commit:** `feat(p1): sports migration, model, and factory (P1-T06)`
**Merged:** 2026-05-21
