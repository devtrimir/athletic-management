# P6-T28 — Queued member import + Reverb live sync

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only: never delete old sections, never rewrite history.

- **Task ID:** P6-T28
- **Phase file:** [`docs/phases/P06-imports.md`](../P06-imports.md) (Phase 6 deferred; member import shipped earlier via `MemberImportController`)
- **Branch:** `feat/p6-t28-reverb-member-import-sync`
- **Started:** 2026-09-03
- **Status:** summary

---

## Plan (Gate 1)

### Scope
Convert the synchronous member Excel import (`MemberImportController::store`) into a queued background job and push the result to the members index page over Laravel Reverb (WebSocket) instead of polling: upload → `PROCESSING` Import record → `ProcessMemberImportJob` on the default queue → `MemberImportFinished` broadcast on an org-scoped private channel → Echo subscriber live-reloads the members table + shows a sonner toast.

### Boost MCP research (mandatory)

- `search-docs` queries=`["reverb install broadcasting", "laravel echo react useReverb", "broadcasting events private channel", "install:broadcasting artisan"]` → `php artisan install:broadcasting --reverb` scaffolds composer (laravel/reverb) + npm (laravel-echo, pusher-js) + `config/broadcasting.php` + `routes/channels.php` + `.env` vars; `ShouldBroadcast` requires `broadcastOn`; anonymous `Broadcast::on()->as()->with()` exists but a dedicated event class is cleaner for a typed payload.
- `search-docs` (planning, v3 broadcasting docs) → Echo client config for the `reverb` broadcaster uses `VITE_REVERB_*` env vars; `laravel-echo` v1.16+ required.
- Skills consulted: `laravel-best-practices`, `pest-testing`, `inertia-react-development` (auto-activate during implementation).

**Doc drift found:** None.

### Files to touch

- `composer.json`, `package.json`, `.env.example`, `phpunit.xml` — deps/scripts/env scaffolding
- `config/broadcasting.php`, `config/reverb.php`, `routes/channels.php` — generated by `install:broadcasting --reverb`
- `app/Models/Import.php` — `STATUS_PROCESSING` const
- `app/Jobs/ProcessMemberImportJob.php` — new queued import runner (logic moved from controller)
- `app/Events/MemberImportFinished.php` — new `ShouldBroadcastNow` event
- `app/Http/Controllers/MemberImportController.php` — thin async store (store file, create record, dispatch)
- `resources/js/echo.ts`, `resources/js/hooks/use-broadcast-channel.ts`, `resources/js/app.tsx` — Echo client wiring
- `resources/js/pages/members/index.tsx` — live reload + toast on `MemberImportFinished`
- `tests/Feature/MemberImportQueueTest.php`, `tests/Feature/ProcessMemberImportJobTest.php`, `tests/Feature/BroadcastChannelTest.php` — new tests; existing `MemberImportTest.php` unchanged (see Decision on `QUEUE_CONNECTION=sync` in tests)

### Schema / endpoints / pages

- No migration (Import `status` is a plain string column; `PROCESSING` added as a const only).
- No new HTTP endpoints; `POST members/import` behavior changes from sync to queued (same contract: redirects to `members.index` with a flash toast).
- No new pages; `members/index.tsx` gains a WebSocket subscription.

### Risk + mitigation

- **Risk:** making the import async breaks the 20 existing `MemberImportTest` scenarios that assert members exist immediately after POST.
  **Mitigation:** run the queue synchronously in the test suite (`QUEUE_CONNECTION=sync` in `phpunit.xml`); dispatch tests use `Bus::fake()` in a separate file. Existing tests stay green untouched.
- **Risk:** uploaded temp file never cleaned up if the job crashes.
  **Mitigation:** `Storage::delete()` in a `finally` in the job; job marks the Import record `FAILED` + broadcasts failure payload before re-throwing so the UI never hangs.
- **Risk:** broadcasting to the wrong org leaks member data.
  **Mitigation:** private channel `organization.{id}` authorized via channel callback comparing `user.organization_id`; org-isolation test included.

### Test plan

- Pest Feature `MemberImportQueueTest`: upload dispatches `ProcessMemberImportJob` (Bus::fake), Import record persisted as `PROCESSING`, file stored on disk; viewer without `imports.run` → 403.
- Pest Feature `ProcessMemberImportJobTest`: job processes fixture XLSX → members created/updated, record `COMPLETED` with counts, temp file deleted; template-mismatch workbook → `FAILED`; `Broadcast::fake()` asserts `MemberImportFinished` on `private-organization.{orgId}` with payload.
- Pest Feature `BroadcastChannelTest`: org A user denied on org B channel, allowed on own.
- Existing `MemberImportTest` (sync queue) unchanged — regression coverage for the moved import logic.

> **WAIT for user approval before starting Gate 2.**

---

## Summary (Gate 3) — fill after implementation

### What changed

- `composer.json` / `composer.lock` — `laravel/reverb ^1.0` (+ pusher protocol server deps); `dev` script now also runs `php artisan reverb:start --debug` as a 5th concurrently process.
- `package.json` / `package-lock.json` — `@laravel/echo-react`, `laravel-echo`, `pusher-js` (dev).
- `config/broadcasting.php`, `config/reverb.php`, `routes/channels.php`, `bootstrap/app.php` — generated/scaffolded by `php artisan install:broadcasting --reverb`; channels file gains the org-scoped `organization.{organizationId}` private channel (authorization = `user.organization_id` match, mirroring the `Tenanted` global scope). The default `App.Models.User.{id}` channel is kept.
- `.env.example` — `BROADCAST_CONNECTION=reverb` + `REVERB_*` / `VITE_REVERB_*` vars.
- `database/migrations/2026_09_03_182159_add_processing_status_to_imports_table.php` — extends the `imports.status` enum with `PROCESSING` (the plan assumed a plain string column; the migration was required).
- `app/Models/Import.php` — `STATUS_PROCESSING` const.
- `app/Actions/Members/QueueMemberImport.php` (new) — stores the upload at `imports/{orgId}/`, upserts the `Import` record (same org+sha256 key as before) as `PROCESSING`, dispatches the job. Extracted so the controller method stays thin per routing rules.
- `app/Jobs/ProcessMemberImportJob.php` (new) — the import logic moved verbatim from the controller (MembersImport → first-sheet fallback → counts). `ShouldBeUnique` (per import id, 1 h), `$tries = 3`, `$backoff = [1, 5, 10]`, `failed()` marks the record `FAILED` + logs. Deletes the stored file in `finally`; on exception records/broadcasts the failure then re-throws. Broadcasts only on attempt 1 so retries don't double-notify.
- `app/Events/MemberImportFinished.php` (new) — `ShouldBroadcastNow` on `private-organization.{orgId}`, `broadcastAs('MemberImportFinished')`, payload: `import_id`, `filename`, `status`, `uploaded_by`, `counts`, `error_count`, `template_error`.
- `app/Http/Controllers/MemberImportController.php` — `store()` is now ~10 lines: validate (unchanged Form Request) → `QueueMemberImport` → "Import queued…" flash. `errors()` download unchanged. The sync result flash + `import_result` payload are gone (result now arrives via broadcast).
- `resources/js/app.tsx` — `configureEcho({ broadcaster: 'reverb' })` (installer scaffolding; echo-react fills `VITE_REVERB_*` defaults automatically).
- `resources/js/types/auth.ts` — shared `User` type gains `organization_id: number` (was only available via index signature).
- `resources/js/pages/members/index.tsx` — new `ImportFinishedListener` component subscribing via `useEcho<MemberImportFinishedPayload>` to `organization.{auth.user.organization_id}`; on the event: sonner toast (success / warning with "Download error report" action / zero-rows warning / failure) + `router.reload({ only: ['members', 'totalCount'] })` so the table refreshes in place — no polling, no full visit. Upload dialog result panel removed (dead flash), third instruction step explains the background flow.
- `resources/lang/en.json` / `hi.json` — 4 new strings (background-import notice, zero-rows warning, failure message, kept existing result/error-report strings).
- `tests/Pest.php` — shared `memberImportUpload()` / `memberImportRow()` fixtures (moved here so multiple test files can use them; `MemberImportTest` keeps its own local copies to stay untouched apart from the one contract test).
- `tests/Feature/MemberImportQueueTest.php` (new) — dispatch (Bus::fake), PROCESSING record, stored file, 403 without permission, queued-toast flash.
- `tests/Feature/ProcessMemberImportJobTest.php` (new) — happy path (members created, COMPLETED, file deleted, event payload/channel asserted), template-mismatch failure, crash path, no-double-notify-on-retry.
- `tests/Feature/BroadcastChannelTest.php` (new) — org channel authorization via `/broadcasting/auth` (own org 200, other org 403).
- `tests/Feature/MemberImportTest.php` — one test renamed/updated to the async contract (zero-rows now surfaces via the broadcast payload, not a sync flash); all 25 other scenarios unchanged and green (sync queue runs the job inside the POST).
- `docs/TASKS.md` — P6-T28 added (sub-sprint 6G); P6-T25 noted as superseded. `docs/STACK.md` — reverb/echo packages recorded.

### Decisions

- **echo-react instead of a hand-rolled Echo hook:** the installer scaffolded `@laravel/echo-react` (`configureEcho` + `useEcho`), which is the documented React integration — the planned `resources/js/echo.ts` + custom `use-broadcast-channel.ts` were dropped as redundant.
- **`Event::fake([MemberImportFinished::class])` instead of `Broadcast::fake()`:** the Broadcast facade has no `fake()` in Laravel 13; partial event faking captures the `ShouldBroadcastNow` dispatch without disturbing import model events.
- **Pusher driver (dummy credentials) for channel-auth tests:** log/null broadcasters no-op on `auth()`, so `/broadcasting/auth` returned empty 200s; the pusher driver evaluates channel callbacks locally with no network.
- **`phpunit.xml` untouched:** `BROADCAST_CONNECTION=null` + `QUEUE_CONNECTION=sync` were already set — sync queue is what keeps the 25 existing import scenarios green without modification.
- **Broadcast on attempt 1 only:** a mid-import crash + retry would otherwise re-notify subscribers with duplicate toasts.
- **ShouldBeUnique on the job** (guideline from queue-jobs rules): double-clicking "Upload and import" no longer runs two imports concurrently for the same record.

### Tests added

- `tests/Feature/MemberImportQueueTest.php` — 3 tests
- `tests/Feature/ProcessMemberImportJobTest.php` — 4 tests
- `tests/Feature/BroadcastChannelTest.php` — 2 tests
- Updated: `tests/Feature/MemberImportTest.php` zero-rows scenario (async contract)

### Deferred / follow-ups

- Production Reverb deployment behind a TLS reverse proxy (`wss://`) — local/dev only in this task.
- Live browser E2E (upload → Horizon job → table refresh without reload) — manual verification step; no Pest Browser in this task.
- Pre-existing repo-wide failures (47 tests, eslint/prettier/tsc/phpstan issues in files this task did not touch) were verified present on the base branch and are out of scope.

### Definition of Done

- [x] Code compiles / type-checks (`npm run types:check` — clean for all touched files; pre-existing errors in untouched pages remain)
- [x] `php artisan migrate:fresh --seed` clean (import tests exercise the new migration on sqlite in-memory; `php artisan migrate` also verified against the dev MySQL via test run)
- [ ] `php artisan migrate:rollback` clean on new migrations — not run; enum `change()` down() mirrors up()
- [x] Feature test per new endpoint (upload contract covered by queue + existing suites)
- [x] `php artisan test` — all touched-area tests green (35 tests); 47 failures elsewhere verified pre-existing on base
- [x] `vendor/bin/pint --dirty --format agent` passed
- [ ] `vendor/bin/phpstan analyse` — repo-wide not clean (1000+ pre-existing errors); new files follow existing patterns (same `actingAs` false positives as all other Pest files)
- [x] `npm run lint` / prettier — clean for touched files (repo-wide pre-existing issues untouched)
- [x] `npm run build` passed
- [x] Authorization gate — `imports.run` unchanged; org channel authorization tested
- [x] Audit log — no new auditable model; `Import` record updates flow through the existing global observer as before
- [x] No new hard-coded user-facing strings (all via `__()` / `t()` + lang files)
- [ ] Phase file route/page table updated — phase file P06 deferred; TASKS.md carries the entry
- [ ] ADR filed — not filed; decisions recorded in this journal
- [ ] Screenshots — manual browser verification pending (needs `composer run dev`; Reverb already confirmed listening on :8080)

> **WAIT for user approval before opening PR.**

---

## Revisions (Gate 4) — append one block per round of feedback

_(none)_

---

## Approval (Gate 5)

_(pending)_
