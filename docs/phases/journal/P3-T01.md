# P3-T01 — Migration: coaches table

- **Task ID:** P3-T01
- **Phase file:** [`docs/phases/P03-coaches.md`](../P03-coaches.md)
- **Branch:** `feat/p3-t01-coaches-migration`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
Create the `coaches` table migration: nullable `member_id` FK, unique `(organization_id, pno)` where pno not null, soft-deletes.

### Boost MCP research (mandatory)
- `database-schema` table=`members` → confirmed `organization_id` FK pattern and `pno` field naming
- Skills consulted: laravel-best-practices SKILL.md

**Doc drift found:** None.

### Files to touch
- `database/migrations/…_create_coaches_table.php` — new migration
- `app/Models/Coach.php` — new model with Tenanted + SoftDeletes
- `database/factories/CoachFactory.php` — new factory
- `tests/Feature/CoachesMigrationTest.php` — schema tests
- `docs/TASKS.md` — tick P3-T01

### Schema / endpoints / pages
- Migration: `coaches` — `id`, `organization_id` FK, `member_id` nullable FK, `full_name_hi`, `full_name_en` nullable, `pno` nullable, `mobile` nullable, `nis_certified` boolean default false, `deleted_at`, timestamps
- UNIQUE `(organization_id, pno)` partial (where pno not null) via `whereNotNull`
- FKs: `organization_id` → organizations (cascade delete), `member_id` → members (set null)

### Test plan
- Pest Feature: migration creates table with correct columns
- Pest Feature: unique constraint on (org, pno)
- Pest Feature: soft-delete works

---

## Summary (Gate 3)

### What changed
- `database/migrations/…_create_coaches_table.php` — coaches schema with partial unique on pno
- `app/Models/Coach.php` — `#[Fillable]`, Tenanted + SoftDeletes, BelongsTo relations
- `database/factories/CoachFactory.php` — standalone + withMember + nisCertified states
- `tests/Feature/CoachesMigrationTest.php` — schema assertions

### Decisions
- **Partial unique on pno:** MySQL doesn't support `WHERE` partial indexes natively via Blueprint; implemented as unique index with `whereNotNull` constraint via raw statement

### Tests added
- `tests/Feature/CoachesMigrationTest.php`

### Deferred / follow-ups
- None

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan migrate:rollback` clean on new migrations
- [x] Feature test per new endpoint
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [x] Phase file route/page table updated

---

## Approval (Gate 5)

## Approved — 2026-05-24
**PR:** #71 — feat/p3-t01-coaches-migration
**Squash commit:** `feat(coaches): coaches migration + schema tests`
**Merged:** 2026-05-24
