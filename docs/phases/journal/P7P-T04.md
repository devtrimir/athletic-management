# P7P-T04 — Player Performance Ranking Report Frontend

- **Task ID:** P7P-T04
- **Phase file:** [`docs/phases/P07-player-performance.md`](../P07-player-performance.md)
- **Branch:** `feat/p7p-t04-performance-report-frontend`
- **Started:** 2026-06-13
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
Add a dedicated report page for `player-performance-ranking` with the existing report filters and a straightforward leaderboard table that shows all scoring data without decorative UI.

### Boost MCP research (mandatory)
- `search-docs` packages=`inertiajs/inertia-laravel,@inertiajs/react,laravel/framework` queries=`inertia react page props tables filters forms`, `inertia react usePage props rendering collections`, `laravel inertia shared page component patterns` → typed page props and plain Inertia page components fit this report well.
- Skills consulted: `inertia-react-development`, `tailwindcss-development`, `wayfinder-development`

**Doc drift found:** None.

### Files to touch
- `app/Http/Controllers/ReportController.php` — render a dedicated page for the new report
- `resources/js/pages/reports/player-performance-ranking.tsx` — table-first report page
- `tests/Feature/Reports/PlayerPerformanceReportTest.php` — assert the dedicated report component
- `docs/phases/journal/P7P-T04.md` — task journal

### Schema / endpoints / pages
- Reuses `GET /reports/{key}` and now renders `reports/player-performance-ranking` for this report key

### Risk + mitigation
- **Risk:** the new report page drifts away from the existing report filters
  **Mitigation:** keep the same filter options and route flow, only changing presentation

### Test plan
- Pest Feature: report page resolves to the dedicated component
- Lint + type-check for the new TSX page

---

## Summary (Gate 3)

### What changed
- `app/Http/Controllers/ReportController.php` — renders a dedicated `reports/player-performance-ranking` page for the ranking report instead of the generic report shell
- `resources/js/pages/reports/player-performance-ranking.tsx` — added a table-first ranking page with report filters, top-N selection, and clear performance columns
- `tests/Feature/Reports/PlayerPerformanceReportTest.php` — now asserts the dedicated report component and default limit payload
- `resources/lang/hi.json` — added missing report UI labels
- `docs/phases/journal/P7P-T04.md` — recorded task plan and outcome

### Decisions
- **Dedicated page, simple presentation:** this report gets its own page so we can label the data properly, but the UI stays intentionally plain and table-focused.
- **Filter actions stay server-driven:** the page continues using the existing report route and filter options rather than introducing a second API path.
- **Top-N uses the same backend limit contract:** the frontend simply drives the `limit` filter already added in `P7P-T03`.

### Tests added
- `tests/Feature/Reports/PlayerPerformanceReportTest.php` — 5 passing tests including dedicated page delivery
- `npx eslint resources/js/pages/reports/player-performance-ranking.tsx` — passes
- `npx prettier --check resources/js/pages/reports/player-performance-ranking.tsx` — passes after formatting

### Deferred / follow-ups
- `P7P-T05` — add the member profile performance tab using the same scoring/report data model
- `npm run types:check` is still blocked by pre-existing TypeScript errors in unrelated files such as `resources/js/components/coach-picker.tsx`, `resources/js/components/date-picker.tsx`, and `resources/js/pages/members/show.tsx`

### Definition of Done
- [ ] Code compiles / type-checks
- [ ] `php artisan migrate:fresh --seed` clean
- [ ] `php artisan migrate:rollback` clean on new migrations
- [x] Feature test for the new report page behavior
- [ ] Unit test per new Service/Action
- [ ] Vitest test per non-trivial component
- [ ] Pest Browser happy-path (if user-facing)
- [x] `php artisan test` passes
- [ ] `vendor/bin/pint --test` passes
- [ ] `vendor/bin/phpstan analyse` passes
- [x] `npx eslint resources/js/pages/reports/player-performance-ranking.tsx` passes
- [ ] `npm run lint` passes
- [ ] `npm run types:check` passes
- [ ] `npm run build` passes
- [x] Authorization gate / Policy added or updated
- [ ] Audit log fires (if applicable)
- [x] No new hard-coded user-facing strings (from P9 onward)
- [x] Phase file route/page table updated
- [ ] ADR filed if non-obvious decision made
- [ ] Screenshots attached (UI changes)
