# P5-T07 — EventController (create event)

## Meta
- **Branch:** `feat/p5-t07-event-controller`
- **Status:** In Progress

## Boost MCP Research
- `search-docs` packages=[laravel/framework] queries=["nested route scopeBindings"] → confirmed `->scopeBindings()` on individual routes; scoping uses the parent model's relationship to constrain the child query.
- `database-schema` filter=events → confirmed columns: tournament_id, sport_id, name_hi, discipline (nullable), weight_category (nullable), gender_class ENUM(M,F,MIXED,OPEN).
- Sport model uses Tenanted scope → sport_id must be validated with org scope in form request.

## Plan
1. `StoreEventRequest` — org-scoped sport_id exists, gender_class in enum
2. `EventController` — store (Gate: update $tournament) + show (Gate: view $tournament)
3. `routes/web.php` — 2 new named routes with `->scopeBindings()`
4. `resources/js/pages/events/show.tsx` — stub for Inertia test discovery
5. `EventControllerTest` — 12 Pest Feature tests

## Files Changed
- `app/Http/Requests/Events/StoreEventRequest.php` — new
- `app/Http/Controllers/EventController.php` — new
- `routes/web.php` — +2 routes
- `resources/js/pages/events/show.tsx` — stub
- `tests/Feature/EventControllerTest.php` — new
