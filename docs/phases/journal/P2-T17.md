# P2-T17 — Pages/Members/Create.tsx

- **Task ID:** P2-T17
- **Phase file:** [`docs/phases/P02-members.md`](../P02-members.md)
- **Branch:** `feat/p2-t17-member-create`
- **Started:** 2026-05-23
- **Status:** merged

---

## Plan (Gate 1)

### Scope
`Pages/Members/Create.tsx` — Inertia `useForm` create form for all 13 `StoreMemberRequest` fields. Server is authoritative (no Zod — not installed).

### Boost MCP research (mandatory)
- `search-docs` packages=[inertiajs/react] queries=[useForm, controlled selects] → `useForm` manages form state; for Radix Select (no native `<select>`) controlled state via `value`+`onValueChange` is required
- Skills consulted: `inertia-react-development`, `tailwindcss-development`, `wayfinder-development`

**Doc drift found:** TASKS.md mentions "Zod mirror" for T17/T18. Zod is not installed and the monolith rule forbids adding packages. Server remains authoritative. Note retained as-is; no separate task needed.

### Files to touch
- `resources/js/pages/members/create.tsx` — replace stub with full useForm form
- `resources/lang/hi.json` — 22 new translation strings

### Schema / endpoints / pages
- Page: `resources/js/pages/members/create.tsx` — Create member form → `POST /members` via `storeMember.url()`

### Risk + mitigation
- **Risk:** shadcn Select (Radix) doesn't emit native form events; needs controlled state
  **Mitigation:** `useForm` with `setData` in `onValueChange` callback

### Test plan
- Deferred to T22–T26 per phase plan

---

## Summary (Gate 3)

### What changed
- `resources/js/pages/members/create.tsx` — Full `useForm` form: 3 card sections (Personal / Service / Player information), 13 fields, shadcn Select for gender/category/level/unit/district, `InputError` per field, `setLayoutProps` breadcrumbs
- `resources/lang/hi.json` — 22 new strings for all UI labels in the form

### Decisions
- **`useForm` over `<Form>`**: required for controlled shadcn Select (Radix UI, no native `<select>` element)
- **No Zod**: not installed; per monolith rule. `StoreMemberRequest` on server is authoritative
- **FK selects as strings**: `home_district_id`/`current_unit_id` stored as strings in `useForm`; Laravel coerces to int for `exists:` validation

### Tests added
- None (deferred to T22–T26)

### Deferred / follow-ups
- T22–T26: full test suite including RBAC × CRUD matrix and Pest Browser E2E

### Definition of Done
- [x] Code compiles / type-checks (`npm run build` → ✓ 2.84s, no errors)
- [x] All strings via `t()` — 22 entries added to `hi.json`
- [x] `vendor/bin/pint --dirty` — no PHP changes in this task
