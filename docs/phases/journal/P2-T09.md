# P2-T09 — Form Requests: StoreMemberRequest, UpdateMemberRequest, ChangeStatusRequest, StoreAliasRequest

- **Task ID:** P2-T09
- **Phase file:** [`docs/phases/P02-members.md`](../P02-members.md)
- **Branch:** `feat/p2-t09-member-form-requests`
- **Started:** 2026-05-23
- **Status:** merged

---

## Plan (Gate 1)

### Scope
"P2-T09 Form Requests: `StoreMemberRequest`, `UpdateMemberRequest`, `ChangeStatusRequest`, `StoreAliasRequest`" — server-side validation for all member write endpoints. Zod frontend mirrors come in T17/T18.

### Boost MCP research (mandatory)
- members migration schema → field list, enums, nullable columns, unique constraints
- member_status_history migration → `status`, `effective_on`, `reason_hi`
- name_aliases migration → `alias_hi`, `source` enum
- Sibling `StoreSportRequest` → conventions: `declare(strict_types=1)`, `App\Http\Requests\<Group>` namespace, `authorize(): true`, `Rule::unique()->where(org_id)` pattern
- Skills consulted: laravel-best-practices

**Doc drift found:** None.

### Files to touch
- `app/Http/Requests/Members/StoreMemberRequest.php` — new
- `app/Http/Requests/Members/UpdateMemberRequest.php` — new
- `app/Http/Requests/Members/ChangeStatusRequest.php` — new
- `app/Http/Requests/Members/StoreAliasRequest.php` — new
- `tests/Feature/StoreMemberRequestTest.php` — new
- `tests/Feature/UpdateMemberRequestTest.php` — new
- `tests/Feature/ChangeStatusRequestTest.php` — new
- `tests/Feature/StoreAliasRequestTest.php` — new
- `docs/TASKS.md` — tick P2-T09

### Risk + mitigation
- **Risk:** `UpdateMemberRequest` route resolver in tests is non-standard — tested via `setRouteResolver` + anonymous class implementing `parameter()`. Verified all 7 update tests pass.

### Test plan
- Pest Feature: 31 tests / 46 assertions covering valid payloads, required field failures, enum rejections, org-scoped PNO uniqueness, self-ignore on update, cross-org PNO passes

> Approved by user ("okay").

---

## Summary (Gate 3)

### What changed
- `app/Http/Requests/Members/` (4 new files) — all `declare(strict_types=1)`, `App\Http\Requests\Members` namespace, `authorize(): true`, PHPDoc return type
- `StoreMemberRequest`: 14 rules; `pno` unique scoped to `organization_id`
- `UpdateMemberRequest`: same fields wrapped in `sometimes`; `pno` unique ignores current member via `->ignore($memberId)`; `$memberId` resolved via `$this->route('member')?->getKey()`
- `ChangeStatusRequest`: 3 rules; `status` in enum, `effective_on` date required, `reason_hi` nullable
- `StoreAliasRequest`: 2 rules; `source` in 5-value enum
- Tests: 4 files, 31 tests, 46 assertions; `StoreAliasRequestTest` uses a Pest dataset for all 5 source values

### Decisions
- **`authorize(): true`:** policy enforced at controller level (T10) — matches project convention
- **`current_status` excluded from StoreMemberRequest:** defaults to `ACTIVE`; status changes go through `ChangeStatusRequest` + `MemberStatusController` (T11)
- **`member_code` excluded from both Store/Update:** generated by `MemberCodeGenerator` in T10 controller

### Test results
401 passed, 9 skipped, 0 failures (31 new tests from this task).
