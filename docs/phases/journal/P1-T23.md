# P1-T23 — Tournament tiers seeder

## Status
Complete

## Plan

**Branch:** `feat/p1-t23-tournament-tiers-seeder`

### Files
- `database/seeders/TournamentTierSeeder.php` — new
- `database/seeders/DatabaseSeeder.php` — add `TournamentTierSeeder::class` before `AdminUserSeeder`
- `tests/Feature/TournamentTierSeederTest.php` — new

### Seeder logic
- `TournamentTier::upsert(6 rows, uniqueBy: ['code'], update: ['label_hi', 'label_en', 'weight'])`
- `tournament_tiers` is a global reference table (no `organization_id`)
- Seeding all 6 ENUM values to avoid orphaned codes with no table row

### Data
| code | label_hi | label_en | weight |
|---|---|---|---|
| INTERNATIONAL | अंतर्राष्ट्रीय | International | 100 |
| NATIONAL | राष्ट्रीय | National | 80 |
| AIPSC | अखिल भारतीय पुलिस खेल | AIPSC | 70 |
| STATE | राज्यस्तरीय | State | 60 |
| ZONAL | क्षेत्रीय | Zonal | 40 |
| OTHER | अन्य | Other | 10 |

### Tests
- All 6 rows seeded with correct code, label_en, weight, non-empty label_hi
- Idempotent (re-run still 6 rows)
- Weights are ordered correctly (100 → 10)

## Boost MCP research
- Checked `tournament_tiers` migration: ENUM('INTERNATIONAL','NATIONAL','AIPSC','STATE','ZONAL','OTHER'), `weight smallInt`, no `organization_id` (global table)
- PHASED_REQUIREMENTS.md line 152: "Tournament tiers (5 standard rows)" — seeded 6 to cover all ENUM values and avoid FK orphans

## Summary

**What was built:** `TournamentTierSeeder` upserts all 6 tournament tier rows (global reference table). Weight field drives prestige ordering in UI dropdowns.

**Tests:** 3 tests, 27 assertions — all pass.

**Files changed:** 3 (new seeder, updated DatabaseSeeder, new test).

**Decisions:**
- Seeded all 6 ENUM values (not just 5) — every ENUM code must have a row since `tournaments.tier_id FK` references this table.
- `weight` descending = prestige order: INTERNATIONAL(100) → OTHER(10).
