# P4-T05 — Form Requests + TeamController (Inertia CRUD)

- **Task ID:** P4-T05
- **Phase file:** [`docs/phases/P04-teams.md`](../P04-teams.md)
- **Branch:** `feat/p4-t05-team-controller`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
`TeamController` with 7 Inertia CRUD actions, `StoreTeamRequest`, `UpdateTeamRequest`, `TeamResource`, resource route, and `teamMembers`/`coachAssignments` HasMany relations on `Team`.

### Boost MCP research (mandatory)
- `read_file app/Http/Controllers/CoachController.php` → confirmed controller pattern (Gate::authorize, QueryBuilder, Inertia::flash, Inertia::defer)
- `read_file app/Http/Requests/Coaches/StoreCoachRequest.php` → confirmed Rule::exists scoped to org, Rule::unique pattern
- `read_file app/Http/Resources/CoachResource.php` → confirmed JsonResource shape pattern
- `grep is_current app/Models/SportSession.php` → confirmed `is_current` boolean field for default session filter
- Skills consulted: laravel-best-practices SKILL.md, inertia-react-development SKILL.md, wayfinder-development SKILL.md

**Doc drift found:** None.

### Files to touch
- `app/Http/Requests/Teams/StoreTeamRequest.php` — new
- `app/Http/Requests/Teams/UpdateTeamRequest.php` — new
- `app/Http/Resources/TeamResource.php` — new
- `app/Http/Controllers/TeamController.php` — new
- `app/Models/Team.php` — add `teamMembers()` + `coachAssignments()` HasMany
- `routes/web.php` — add `Route::resource('teams', TeamController::class)`
- `docs/TASKS.md` — tick P4-T05

### Schema / endpoints / pages
- `GET /teams` → teams/index (session filter default=current, withCount players/coaches, paginate 25)
- `GET /teams/create` → teams/create
- `POST /teams` → store → redirect show
- `GET /teams/{team}` → teams/show (Deferred counts)
- `GET /teams/{team}/edit` → teams/edit
- `PATCH /teams/{team}` → update → redirect show
- `DELETE /teams/{team}` → soft-delete → redirect index

### Risk + mitigation
- **Risk:** Unique validation in UpdateTeamRequest must fall back to existing FK values when not re-submitted
  **Mitigation:** Use `$this->route('team')?->sport_id` etc. as fallback in `->where()` clauses

### Test plan
- Deferred to P4-T13 (Inertia page stubs don't exist yet — following P3-T04 pattern)

---

## Summary (Gate 3)

### What changed
- `app/Http/Requests/Teams/StoreTeamRequest.php` — `sport_id`/`session_id`/`unit_id` via `Rule::exists` scoped to org; `name_hi` unique on `(org, sport, session, unit, name_hi)`; `in_charge_hi` nullable
- `app/Http/Requests/Teams/UpdateTeamRequest.php` — `sometimes` rules; unique ignores current record; FK fallback to existing model values
- `app/Http/Resources/TeamResource.php` — `whenCounted` for `players_count`/`coaches_count`; `whenLoaded` for sport/session/unit
- `app/Http/Controllers/TeamController.php` — 7 actions; `index` filters by current session by default via `SportSession::where('is_current', true)->value('id')`; `show` uses `Inertia::defer` for counts
- `app/Models/Team.php` — added `teamMembers(): HasMany` and `coachAssignments(): HasMany`
- `routes/web.php` — `Route::resource('teams', TeamController::class)` added; Wayfinder `TeamController.ts` generated

### Decisions
- **Default session filter:** `SportSession::where('organization_id', $orgId)->where('is_current', true)->value('id')` — passed to frontend as `defaultSessionId` prop so UI can reflect it
- **Tests deferred:** Inertia page components don't exist yet; full tests deferred to T13 following P3-T04 pattern

### Deferred / follow-ups
- Tests → P4-T13

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [x] Authorization gate / Policy added or updated
- [x] `npm run lint` passes (Wayfinder TS generated)

---

## Approval (Gate 5)

## Approved — 2026-05-24
**PR:** #82 — feat/p4-t05-team-controller
**Squash commit:** `feat(teams): TeamController, form requests, resource, routes`
**Merged:** 2026-05-24
