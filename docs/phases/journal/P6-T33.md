# P6-T33 — Remove duplicate header on coach listing print page

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only.

- **Task ID:** P6-T33
- **Branch:** `feat/p6-t33-coach-print-header`
- **Started:** 2026-09-04
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
The coach listing print page rendered the report title twice: the letterhead subtitle "Coach Listing Report" (line ~468 of CoachController::buildPrintHtml) and a duplicate `<h1>Coach Listing</h1>` directly above the table. Remove the `<h1>` and its now-unused CSS rule. The letterhead (org title + report subtitle) is the single header. User-approved direction: "it should be one".

### Research
- No `search-docs` needed (pure HTML-string removal, no framework behavior).
- Convention check: `TournamentExportController` also has letterhead + h1, but its h1 carries event context lines beneath it — not a duplicate there. Only the coach listing h1 was redundant.
- No tests assert on "Coach Listing" HTML.

### Files to touch
- `app/Http/Controllers/CoachController.php` — `buildPrintHtml()`: remove `'<h1>'.e(__('Coach Listing')).'</h1>'` and the `h1{...}` CSS rule.

### Risk + mitigation
- **Risk:** print tests may assert the h1. **Mitigation:** grep confirmed none; print test failures before/after are the 4 known pre-existing ones.

### Test plan
- Existing print tests must not regress beyond the 4 known pre-existing failures; pint clean.

> User approved 2026-09-04. Gate 2 done same session (single-hunk change).

---

## Summary (Gate 3)

### What changed
- `app/Http/Controllers/CoachController.php` — removed duplicate `h1` header + unused `h1` CSS rule from the coach listing print HTML. Printed page now shows the letterhead ("UPPSCB" / "Coach Listing Report") once, followed directly by the table.

### Decisions
- Kept the letterhead as the single header (it already carries the report title); removed the redundant `<h1>` rather than the letterhead subtitle, matching the user's "it should be one".

### Tests added
- None needed — no behavior change beyond HTML output; no test asserted the h1.

### Verification
- `vendor/bin/pint --dirty --format agent` passed.
- `--filter=print` on CoachControllerTest: 5 passed / 4 failed — the same 4 failures as `main` (pre-existing).

### Definition of Done
- [x] Pint passes
- [x] No new test failures

---

## Approval (Gate 5)

## Approved — 2026-09-04 by user
**PR:** #184 — https://github.com/devtrimir/athletic-management/pull/184
**Merge commit:** `3b8cbf6`
**Merged:** 2026-09-04
**Post-merge:** branch deleted locally and remotely.
