# P4-T09 — Pages/Teams/Index.tsx (default-current-session filter)

- **Task ID:** P4-T09
- **Phase file:** `docs/TASKS.md` (Phase 4 — Frontend)
- **Branch:** `feat/p4-t09-teams-index-page`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
Build `resources/js/pages/teams/index.tsx` — paginated teams table with
default-current-session filter, sport filter, unit filter, and text search.
Task line: *`Pages/Teams/Index.tsx` (default-current-session filter)*.

### Boost MCP research (mandatory)
- Skills consulted: `inertia-react-development/SKILL.md`, `tailwindcss-development/SKILL.md`, `wayfinder-development/SKILL.md`
- `TeamController::index` already passes `teams` (paginated), `filters`, `defaultSessionId` — no backend changes needed.
- Wayfinder `TeamController.ts` exports `index.url()`, `create.url()`, `show.url(team)` — available.
- `resources/lang/hi.json` already has `"Teams": "टीम"` — new strings needed below.

**Doc drift found:** None.

### Files to touch
- `resources/js/pages/teams/index.tsx` — **create** new page
- `resources/lang/hi.json` — add new translation strings

### Pages / props contract
Props from `TeamController::index`:
```ts
{
  teams: PaginatedTeams,       // paginated, each item has sport, session, unit, players_count, coaches_count
  filters: {
    session_id?: string,
    sport_id?: string,
    unit_id?: string,
    q?: string,
  },
  defaultSessionId: number | null,
}
```

Team row shape:
```ts
{
  id: number,
  name_hi: string,
  in_charge_hi: string | null,
  players_count: number,
  coaches_count: number,
  sport: { id: number; name: string } | null,
  session: { id: number; name: string } | null,
  unit: { id: number; name_hi: string } | null,
}
```

### UI structure
- Heading + "New team" button (right)
- Filter bar: text search, session_id Select (passed via shared props `sessions`), sport_id Select (shared props `sports`), unit_id Select (shared props `units`), Clear button
- NOTE: `sessions`, `sports`, `units` are available in shared Inertia props from `HandleInertiaRequests`; if not, fall back to no select (filter by ID via URL only). **Check shared props first.**
- Table columns: Name (Hindi), Sport, Session, Unit, In-charge, Players, Coaches, Actions (Eye icon)
- Empty state message
- Pagination (same pattern as Members/Coaches index)
- breadcrumbs layout via `TeamsIndex.layout`

### New translation strings needed
```json
"Manage teams": "टीम का प्रबंधन करें",
"New team": "नई टीम",
"Search teams…": "टीम खोजें…",
"No teams match your filters.": "कोई टीम फ़िल्टर से मेल नहीं खाती।",
"No teams yet.": "अभी कोई टीम नहीं।",
"All sessions": "सभी सत्र",
"All sports": "सभी खेल विधाएं",
"All units": "सभी इकाइयां",
"In-charge": "प्रभारी",
"Players": "खिलाड़ी",
"Sport": "खेल विधा",
"Session": "सत्र",
```

### Risk + mitigation
- **Risk:** Shared props may not include sessions/sports/units for selects.
  **Mitigation:** Check `HandleInertiaRequests` — if not shared, pass from controller OR use simple text input for those filters for now (P4-T09 scope only; enhancement later).

### Test plan
- Tests deferred to P4-T13/T14 (same pattern as P2/P3 frontend tasks).

> **WAIT for user approval before starting Gate 2.**

---

## Summary (Gate 3) — fill after implementation

---

## Revisions (Gate 4)

---

## Approval (Gate 5)
