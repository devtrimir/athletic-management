# P4-T08 — API: GET /api/v1/members/{member}/teams + GET /api/v1/coaches/{coach}/teams

- **Task ID:** P4-T08
- **Phase file:** [`docs/phases/P04-teams.md`](../P04-teams.md)
- **Branch:** `feat/p4-t08-member-coach-teams-api`
- **Started:** 2026-05-24
- **Status:** planning

---

## Plan (Gate 1)

### Scope
Two read-only JSON endpoints returning historical team memberships:
- `GET /api/v1/members/{member}/teams` — all `team_members` records for the member with team + sport + session info (drives member profile Teams tab)
- `GET /api/v1/coaches/{coach}/teams` — all `coach_assignments` records for the coach with team + sport + session info

### Boost MCP research (mandatory)
- `read_file app/Http/Controllers/Api/V1/CoachSearchController.php` → confirmed invokable controller + JsonResource + `Gate::authorize` pattern
- `database-schema` tables=`team_members`, `coach_assignments`, `teams` → confirmed FKs and fields
- Skills consulted: laravel-best-practices SKILL.md

**Doc drift found:** None.

### Files to touch
- `app/Http/Controllers/Api/V1/MemberTeamsController.php` — new invokable
- `app/Http/Resources/Api/V1/MemberTeamResource.php` — new
- `app/Http/Controllers/Api/V1/CoachTeamsController.php` — new invokable
- `app/Http/Resources/Api/V1/CoachTeamResource.php` — new
- `routes/api.php` — add 2 routes under `v1.*`
- `docs/TASKS.md` — tick P4-T08

### Schema / endpoints / pages
- `GET /api/v1/members/{member}/teams` → `{ data: [{ team_member_id, role, joined_on, left_on, team: { id, name_hi }, sport: { id, name }, session: { id, name } }] }`
- `GET /api/v1/coaches/{coach}/teams` → `{ data: [{ assignment_id, role, team: { id, name_hi }, sport: { id, name }, session: { id, name } }] }`

### Risk + mitigation
- **Risk:** Contract must be preserved when Meilisearch is swapped in P8
  **Mitigation:** Use stable JsonResource shape; no raw arrays

### Test plan
- Pest Feature: returns correct memberships for member
- Pest Feature: returns empty array for member with no teams
- Pest Feature: does not return other org's teams
- Pest Feature: same for coach endpoints

> **WAIT for user approval before starting Gate 2.**

---

## Summary (Gate 3) — fill after implementation

---

## Revisions (Gate 4)

---

## Approval (Gate 5)
