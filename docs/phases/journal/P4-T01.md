# P4-T01 — Migration: teams table

- **Task ID:** P4-T01
- **Phase file:** [`docs/phases/P04-teams.md`](../P04-teams.md)
- **Branch:** `feat/p4-t01-teams-migration`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
Create the `teams` table migration: FKs to organizations/sports/sport_sessions/units, unique composite `(organization_id, sport_id, session_id, unit_id, name_hi)`, soft-deletes.

### Boost MCP research (mandatory)
- `database-schema` tables=`sports`, `sport_sessions`, `units` → confirmed PK/FK naming
- Skills consulted: laravel-best-practices SKILL.md

**Doc drift found:** None.

### Files to touch
- `database/migrations/…_create_teams_table.php` — new
- `app/Models/Team.php` — new with Tenanted + SoftDeletes
- `database/factories/TeamFactory.php` — new with `forOrganization()` state
- `tests/Feature/TeamsMigrationTest.php` — schema tests
- `docs/TASKS.md` — tick P4-T01

### Schema / endpoints / pages
- `teams`: `id`, `organization_id`, `sport_id`, `session_id`, `unit_id`, `name_hi`, `in_charge_hi` nullable, `deleted_at`, timestamps
- UNIQUE `teams_unique_org_sport_session_unit_name` on `(organization_id, sport_id, session_id, unit_id, name_hi)`
- Indexes on `organization_id` and `session_id`

### Test plan
- Pest Feature: table has correct columns
- Pest Feature: unique constraint enforced
- Pest Feature: soft-delete works
- Pest Feature: `forOrganization` factory state creates correct FK chain

---

## Summary (Gate 3)

### What changed
- `database/migrations/…_create_teams_table.php` — teams schema
- `app/Models/Team.php` — `#[Fillable]`, Tenanted + SoftDeletes, BelongsTo relations to Organization/Sport/SportSession/Unit
- `database/factories/TeamFactory.php` — default state + `forOrganization(Organization $org)` state
- `tests/Feature/TeamsMigrationTest.php` — 7 schema/factory tests

### Decisions
- **`forOrganization()` state:** creates all FK records within the same org to avoid cross-org constraint violations in tests

### Tests added
- `tests/Feature/TeamsMigrationTest.php` (7 tests, 20 assertions)

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan migrate:rollback` clean
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes

---

## Approval (Gate 5)

## Approved — 2026-05-24
**PR:** #80 — feat/p4-t01-teams-migration
**Squash commit:** `feat(teams): teams migration, model, factory, and migration tests (P4-T01)`
**Merged:** 2026-05-24
