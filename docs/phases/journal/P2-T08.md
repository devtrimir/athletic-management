# P2-T08 — MemberPolicy rules wired to permissions

- **Task ID:** P2-T08
- **Phase file:** [`docs/phases/P02-members.md`](../P02-members.md)
- **Branch:** `feat/p2-t08-member-policy`
- **Started:** 2026-05-23
- **Status:** merged

---

## Plan (Gate 1)

### Scope
"P2-T08 `MemberPolicy` rules wired to permissions" — complete the stub policy so every method delegates to a `members.*` permission code (no role checks). Add the missing `restore` method and full test coverage in `PolicyStubsTest`.

### Boost MCP research (mandatory)
- Codebase inspection → `app/Policies/MemberPolicy.php` already had viewAny/view/create/update/delete as a stub. `CoachPolicy` and siblings use `mixed` for the model arg — followed that convention.
- `app/Auth/Rbac.php` + `app/Providers/AuthServiceProvider.php` → `Gate::before()` delegates `$user->can($ability)` to `Rbac::userHasPermission()`. No changes needed to the RBAC layer.
- `analysis/report/PHASED_REQUIREMENTS.md` line 126 → canonical permission codes: `members.view`, `members.create`, `members.update`, `members.delete`, `members.restore`.
- Skills consulted: laravel-best-practices

**Doc drift found:** None.

### Files to touch
- `app/Policies/MemberPolicy.php` — add `declare(strict_types=1)`, add `restore()` method
- `tests/Feature/PolicyStubsTest.php` — add `MemberPolicy::view` and `MemberPolicy::restore` dataset entries
- `docs/TASKS.md` — tick P2-T08

### Risk + mitigation
- **Risk:** `restore` permission code not yet seeded in DB — fine; `PolicyStubsTest` creates permissions on-the-fly via `Permission::firstOrCreate`.

### Test plan
- Pest Feature: extend `PolicyStubsTest` dataset with `view` and `restore` entries → 4 new test runs (× 2 scenarios = pass/fail)

> Approved by user ("shoot").

---

## Summary (Gate 3)

### What changed
- `app/Policies/MemberPolicy.php` — added `declare(strict_types=1)`; added `restore(User $user, mixed $member): bool` → `members.restore`
- `tests/Feature/PolicyStubsTest.php` — added `MemberPolicy::view` and `MemberPolicy::restore` to the dataset; pint fixed `binary_operator_spaces`
- `docs/TASKS.md` — ticked P2-T08

### Decisions
- **`mixed` vs `Member` type hint:** kept `mixed` to match all sibling policies (CoachPolicy, TeamPolicy, etc.) — uniformity over strictness. Can be tightened when sibling policies are updated.
- **`changeStatus` / `manageAlias` deferred:** these are needed by T11/T12 controllers; adding them there keeps each task self-contained and avoids shipping unused policy gates.

### Test results
370 passed, 9 skipped, 0 failures (4 new assertions from this task).
