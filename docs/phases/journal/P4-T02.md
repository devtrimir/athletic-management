# P4-T02 + P4-T03 — Migrations: team_members + coach_assignments

- **Task ID:** P4-T02, P4-T03
- **Phase file:** [`docs/phases/P04-teams.md`](../P04-teams.md)
- **Branch:** `feat/p4-t02-t03-team-members-coach-assignments`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
- **T02:** `team_members` pivot migration + model + factory (unique `(team_id, member_id)`)
- **T03:** `coach_assignments` pivot migration + model + factory (unique `(team_id, coach_id, role)`)

Bundled as structural twins with identical patterns and no cross-dependency.

### Boost MCP research (mandatory)
- `database-schema` tables=`teams`, `members`, `coaches`, `sport_sessions` → confirmed FK targets
- Skills consulted: laravel-best-practices SKILL.md

**Doc drift found:** None.

### Files to touch
- `database/migrations/…_create_team_members_table.php` — new
- `app/Models/TeamMember.php` — new (no Tenanted, no SoftDeletes)
- `database/factories/TeamMemberFactory.php` — new (captain/reserve states)
- `database/migrations/…_create_coach_assignments_table.php` — new
- `app/Models/CoachAssignment.php` — new (no Tenanted, no SoftDeletes)
- `database/factories/CoachAssignmentFactory.php` — new (head/assistant states)
- `tests/Feature/TeamPivotMigrationTest.php` — schema + factory tests
- `docs/TASKS.md` — tick P4-T02, P4-T03

### Schema / endpoints / pages
- `team_members`: `team_id` FK (cascade delete), `member_id` FK (cascade delete), `session_id` FK, `role` ENUM(PLAYER,CAPTAIN,RESERVE) default PLAYER, `joined_on` date nullable, `left_on` date nullable
- `coach_assignments`: `team_id` FK (cascade delete), `coach_id` FK (cascade delete), `session_id` FK, `role` ENUM(HEAD,ASSISTANT)

### Test plan
- Pest Feature: both tables have correct columns/constraints
- Pest Feature: factory states produce correct role values
- Pest Feature: cascade delete works

---

## Summary (Gate 3)

### What changed
- `database/migrations/…_create_team_members_table.php`
- `app/Models/TeamMember.php` — `#[Fillable]`, HasFactory, date casts, BelongsTo team/member/session
- `database/factories/TeamMemberFactory.php` — `captain()`, `reserve()` states
- `database/migrations/…_create_coach_assignments_table.php`
- `app/Models/CoachAssignment.php` — `#[Fillable]`, HasFactory, BelongsTo team/coach/session
- `database/factories/CoachAssignmentFactory.php` — `head()`, `assistant()` states
- `tests/Feature/TeamPivotMigrationTest.php` — 11 tests, 27 assertions

### Decisions
- **No Tenanted/SoftDeletes on pivot models:** pivot rows cascade-delete with their parent team; no independent soft-delete needed
- **cascadeOnDelete on both FKs:** team_id and member_id/coach_id both cascade to keep data clean

### Tests added
- `tests/Feature/TeamPivotMigrationTest.php` (11 tests, 27 assertions)

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan migrate:fresh --seed` clean
- [x] `php artisan migrate:rollback` clean
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes

---

## Approval (Gate 5)

## Approved — 2026-05-24
**PR:** #81 — feat/p4-t02-t03-team-members-coach-assignments
**Squash commit:** `feat(teams): team_members + coach_assignments migrations, models, factories, tests (P4-T02, P4-T03)`
**Merged:** 2026-05-24
