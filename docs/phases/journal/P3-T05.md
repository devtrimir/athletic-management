# P3-T05 — API: GET /api/v1/search/coaches

- **Task ID:** P3-T05
- **Phase file:** [`docs/phases/P03-coaches.md`](../P03-coaches.md)
- **Branch:** `feat/p3-t05-coach-search-api`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
`GET /api/v1/search/coaches?q=...` — MySQL FULLTEXT search on `full_name_hi` (ngram), returns top 50. Used by autocomplete pickers. Contract matches member search endpoint shape.

### Boost MCP research (mandatory)
- `search-docs` packages=[laravel] queries=["fulltext search ngram"] → confirmed FULLTEXT index with ngram parser pattern
- `database-schema` table=`coaches` → `full_name_hi`, `organization_id`
- Skills consulted: laravel-best-practices SKILL.md

**Doc drift found:** None.

### Files to touch
- `app/Http/Controllers/Api/V1/CoachSearchController.php` — new
- `routes/api.php` — add GET `/v1/search/coaches`
- `tests/Feature/Api/V1/CoachSearchTest.php` — new

### Schema / endpoints / pages
- `GET /api/v1/search/coaches?q=` — returns `[{ id, full_name_hi, pno }]` top 50, org-scoped

### Test plan
- Pest Feature: returns coaches matching query
- Pest Feature: does not return other org's coaches
- Pest Feature: empty q returns empty array

---

## Summary (Gate 3)

### What changed
- `app/Http/Controllers/Api/V1/CoachSearchController.php` — FULLTEXT search with SQLite LIKE fallback
- `routes/api.php` — `GET /v1/search/coaches`
- `tests/Feature/Api/V1/CoachSearchTest.php` — search tests

### Decisions
- **SQLite fallback:** test env uses LIKE search; MySQL uses FULLTEXT; same service pattern as MemberSearchService

### Tests added
- `tests/Feature/Api/V1/CoachSearchTest.php`

### Definition of Done
- [x] Code compiles / type-checks
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [x] Authorization gate / Policy added or updated

---

## Approval (Gate 5)

## Approved — 2026-05-24
**PR:** #76 — feat/p3-t05-coach-search-api
**Squash commit:** `feat(coaches): GET /api/v1/search/coaches endpoint`
**Merged:** 2026-05-24
