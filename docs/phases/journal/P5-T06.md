# P5-T06 — Form Requests + TournamentController (Inertia CRUD)

## Meta
- **Branch:** `feat/p5-t06-tournament-controller`
- **Phase file:** `docs/phases/P05-tournaments-events.md`
- **Status:** In Progress

## Boost MCP Research
- `search-docs` packages=[laravel/framework, inertiajs/inertia-laravel, spatie/laravel-query-builder] → confirmed form request pattern, Inertia render, QueryBuilder paginate all consistent with existing TeamController usage.
- `database-schema` filter=tournament_tiers → columns: code (enum), label_hi, label_en, weight. No org-scope.
- `database-schema` filter=tournaments → columns match model: org_id, session_id, tier_id, sport_id (nullable), name_hi, venue, date_from, date_to, raw_date_text, softDeletes.

## Plan
1. `StoreTournamentRequest` — validation with org-scoped exists checks
2. `UpdateTournamentRequest` — same + `sometimes`, ignore self on unique
3. `TournamentResource` — API resource wrapping tournament + whenLoaded
4. `TournamentController` — 7 methods (index, create, store, show, edit, update, destroy)
5. `routes/web.php` — add resource route
6. `TournamentControllerTest` — 16 Pest Feature tests

## Files Changed
- `app/Http/Controllers/TournamentController.php` — new
- `app/Http/Requests/Tournaments/StoreTournamentRequest.php` — new
- `app/Http/Requests/Tournaments/UpdateTournamentRequest.php` — new
- `app/Http/Resources/TournamentResource.php` — new
- `routes/web.php` — add `Route::resource('tournaments', TournamentController::class)`
- `tests/Feature/TournamentControllerTest.php` — new
