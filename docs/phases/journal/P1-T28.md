# P1-T28 — Districts CRUD

- **Task ID:** P1-T28
- **Phase file:** [`docs/phases/P01-reference-data.md`](../P01-reference-data.md)
- **Branch:** `feat/p1-t28-districts-crud`
- **Started:** 2025-07-23
- **Status:** merged

---

## Plan (Gate 1)

### Scope
Districts CRUD slice under Settings. Global reference table (no organization_id), globally-unique `code`. Fields: name_hi, name_en, state, code.

### Boost MCP research (mandatory)
- `database-schema` table=`districts` → id, name_hi, name_en, state, code UNIQUE, timestamps — NO organization_id
- `database-schema` table=`district_factory` → 8 UP districts using fake()->unique()

**Doc drift found:** None

### Files to touch
- `app/Http/Requests/Settings/StoreDistrictRequest.php` — new
- `app/Http/Requests/Settings/UpdateDistrictRequest.php` — new
- `app/Policies/DistrictPolicy.php` — new (no org-scope)
- `app/Http/Controllers/Settings/DistrictController.php` — new (no org filter)
- `routes/settings.php` — add DistrictController + resource route
- `resources/js/pages/settings/districts/index.tsx` — new
- `resources/js/pages/settings/districts/create.tsx` — new
- `resources/js/pages/settings/districts/edit.tsx` — new
- `tests/Feature/Settings/DistrictCrudTest.php` — new

### Schema / endpoints / pages
- Endpoint: `GET /settings/districts` — index
- Endpoint: `GET /settings/districts/create` — create form
- Endpoint: `POST /settings/districts` — store
- Endpoint: `GET /settings/districts/{district}/edit` — edit form
- Endpoint: `PATCH /settings/districts/{district}` — update
- Endpoint: `DELETE /settings/districts/{district}` — destroy

### Risk + mitigation
- **Risk:** factory only has 8 unique entries — exhaustion in tests
  **Mitigation:** use explicit values in tests, not bulk factory defaults

### Test plan
- Pest Feature: index 200, create 200, store creates row, store validates, store 403, update saves, update same-code self, update validates, update 403, destroy deletes, destroy 403, guest redirects

---

## Summary (Gate 3)

### What changed
- `StoreDistrictRequest` — required fields, globally-unique code rule
- `UpdateDistrictRequest` — same, with `->ignore($districtId)` on code
- `DistrictPolicy` — all methods check `reference_data.manage` only; no org-scope (districts are global)
- `DistrictController` — CRUD, no org filter on index, no organization_id in store data
- `routes/settings.php` — DistrictController import + `Route::resource('settings/districts', ...)->names('districts')`
- React pages — index (table with name_hi/name_en/state/code), create (4 inputs, state defaults to "Uttar Pradesh"), edit (pre-filled + `setLayoutProps`)
- Pest Feature test — 16 tests, all pass

### Decisions
- **No organization_id scope:** Districts are global government reference data, not org-owned.
- **state defaults to "Uttar Pradesh":** All 75 districts are UP districts; saves user typing.
