# P2C-T02 — Member posting XOR district + initial rank + aligned print/export/import

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only: never delete old sections, never rewrite history.

- **Task ID:** P2C-T02
- **Phase file:** `docs/phases/P02-members.md` (P2C subsection to be appended at approval gate; TASKS.md entry same commit)
- **Branch:** `feat/p2c-t02-member-posting-rank-print-alignment`
- **Started:** 2026-09-03
- **Status:** planning

---

## Plan (Gate 1)

### Scope
Four linked member-module changes requested by the user:
1. `current_unit_id` and `posting_district_id` become mutually exclusive (posted at a unit OR dedicated to a district — never both), enforced server-side and in the form.
2. Remove the `appointment` field; replace with `initial_rank` (rank at hiring). Existing `rank` continues to show the current rank.
3. Apply the same change everywhere: member show/edit/create UI, per-member print preview, member listing print, Excel export, and the sample import template + import parser.
4. Add a member **listing print** (`GET /members/print`) with the official global header (UPPSCB letterhead + watermark, same as `coaches.print`), whose columns match the Excel export exactly.

### Boost MCP research (mandatory)
- `search-docs` packages=[laravel/framework] queries=["validation prohibits rule mutually exclusive fields", "form request validation conditional rules"] → `prohibits:anotherfield` is built for exactly this: if the field under validation is not missing/empty (null, `''`, empty array all count as empty), the other field must be missing/empty. Confirmed usable on both `current_unit_id` and `posting_district_id` for two-way XOR. `exclude_*`/`prohibited_*` variants not needed.
- `database-schema` filter=`members` → empty result (Boost connected to a DB without the table); used the canonical migration `database/migrations/2026_05_22_125701_create_members_table.php` instead. Columns confirmed: `rank` (string 100, nullable), `appointment` (string 255, nullable), `current_unit_id` FK units, `posting_district_id` FK districts nullOnDelete, `home_district_id` FK districts.
- Skills consulted: `member-module-structure` (module map), `pest-testing`, `inertia-react-development`, `laravel-best-practices`, `wayfinder-development`, `product-ui-ux-standards` (followed during implementation).

**Doc drift found:** None for this task. (Side note, not in scope: `docs/TASKS.md` checkboxes for P2B/P5M/P7N are stale — features exist in code but boxes are unticked. Flag for a docs-cleanup task later.)

### Existing feature check (what's there today)
- Fields: `members.rank` (free text; forms mirror it with a ranks-master combobox + "__other__" custom fallback), `members.appointment` (free text). `appointment` is referenced in: Store/UpdateMemberRequest, MemberResource, MembersImport, MemberImportSchema, MemberExportController, AuditLogBuilder, MemberFactory, MemberSeeder, CoachSeeder, member-quick-view, members create/edit/index/show/print-preview pages, MemberImportTest.
- Posting: `current_unit_id` and `posting_district_id` are independent nullable fields; no exclusion logic anywhere. Export already merges them into one "Posting Unit / District" column (`postingDistrict?->name ?? currentUnit?->name`).
- Export: `MemberExportController` (`members.export`, `members.export.show`) — Spatie QueryBuilder filters + `COLUMN_LABELS` + `ReportExport`.
- Import: `MemberImportSchema::columns()` is the single contract for the sample template (`members.import.template`) and `MembersImport` (position-mapped). `appointment` column exists at index 14; `unit` and `posting_district` are separate `ref` dropdown columns.
- Print: per-member print preview exists (`members.preview` → `members/print-preview.tsx`). **No member listing print route exists** — `coaches.print` (`CoachController::print`, inline-CSS HTML letterhead + `/logo.jpg` watermark) is the model to mirror.

### Files to touch
Backend:
- `database/migrations/2026_09_03_000001_add_initial_rank_to_members_table.php` — add nullable `initial_rank` string(100). `appointment` column left in place (non-destructive; per WORKFLOW §8 column drops ship a release later). All code stops reading/writing it.
- `app/Models/Member.php` — fillable + docblock: add `initial_rank`.
- `app/Http/Requests/Members/StoreMemberRequest.php` / `UpdateMemberRequest.php` — add `initial_rank` (nullable string max 100); remove `appointment` rule; add two-way `prohibits` XOR on `current_unit_id` / `posting_district_id`.
- `app/Http/Resources/MemberResource.php` — swap `appointment` → `initial_rank`.
- `app/Imports/MembersImport.php` — map `initial_rank` column; row error when both `unit` and `posting_district` cells are filled.
- `app/Support/Members/MemberImportSchema.php` — replace `appointment` column with `initial_rank` ("Initial Rank / भर्ती पद") **at the same position** (index 14).
- `app/Http/Controllers/MemberExportController.php` — `COLUMN_LABELS`: `appointment` → `initial_rank` ("Initial Rank"); posting column unchanged (already merged).
- `app/Http/Controllers/MemberController.php` — add `print(Request)` for `members.print`, mirroring `CoachController::print`: official letterhead + watermark, columns = export default columns, respects the same query filters as `members.export`.
- `app/Support/Reports/OfficialPrintLayout.php` (new) — extract the shared letterhead/CSS builder currently string-copied in `CoachController::print` and `TournamentExportController`; coach/tournament switch to it with zero behavior change (their tests stay green).
- `routes/web.php` — `GET members/print` → `members.print`, placed with the other members routes before parameterized member routes (mirrors `coaches/print` placement).
- `app/Services/AuditLogBuilder.php` — Member field label map: `appointment` → `initial_rank` ("Initial rank").

Frontend:
- `resources/js/pages/members/create.tsx` / `edit.tsx` — remove Appointment input; add Initial Rank combobox mirroring the Rank field (ranks master + "__other__"); unit/posting-district pickers: selecting one clears + disables the other with helper text; all strings via `t()`.
- `resources/js/pages/members/show.tsx` — replace Appointment row with Initial Rank.
- `resources/js/components/members/member-quick-view.tsx` — same swap.
- `resources/js/pages/members/index.tsx` — column def `appointment` → `initial_rank`; add Print button next to Export passing current filters to `members.print`.
- `resources/js/pages/members/print-preview.tsx` — swap Appointment → Initial Rank.
- Wayfinder regen: `php artisan wayfinder:generate --with-form --no-interaction`.

Tests:
- New `tests/Feature/MemberPostingAndRankTest.php` — XOR validation, initial_rank persist (create/update), cross-org safety.
- `tests/Feature/MemberImportTest.php` — initial_rank mapping; row error when unit + posting_district both filled; template headings contain Initial Rank, not Appointment.
- `tests/Feature/MemberExportControllerTest.php` (existing, extend) — Initial Rank column present / Appointment absent; posting merged value.
- New print tests (in the new test file) — `members.print` 200, letterhead text, headings match export column order, filters respected, 403 without permission.

### Schema / endpoints / pages
- Migration: `add_initial_rank_to_members_table` — nullable string(100) `initial_rank` on `members`. No index.
- Endpoint: `GET /members/print` (`members.print`) — server-rendered HTML listing print with official header; same filter query params as `members.export`.
- Page: no new React page (print is HTML like coaches); existing member pages edited as listed.

### Risk + mitigation
- **Risk:** import template column order is a frozen contract — old downloaded sheets break if headings shift. **Mitigation:** `initial_rank` replaces `appointment` at the same position (index 14); import validates headings, so old sheets fail loudly with a heading-mismatch error instead of silently mis-importing. Called out for the user below.
- **Risk:** `initial_rank` is NULL for existing rows (nothing backfills it — see open question). **Mitigation:** optional in forms; print/export show blank.
- **Risk:** extracting the shared print layout could change coach/tournament print output. **Mitigation:** extract verbatim, coach/tournament tests must stay byte-equivalent (note: 5 coach print tests already fail on `main` for unrelated heading-text drift — those stay as-is, not fixed here).
- **Risk:** rows that already have BOTH unit and posting district set (legacy/import data) would become un-editable until one is cleared. **Mitigation:** count such rows during implementation; XOR only blocks new writes; journal notes the data-cleanup step.

### Open questions (user to confirm with approval)
1. **Backfill `initial_rank`?** If existing `appointment` data actually stores hiring ranks, migration can backfill `initial_rank = appointment` (portable SQL). If not, leave NULL. **Default: leave NULL.**
2. **Import template:** replacing "Appointment / नियुक्ति" with "Initial Rank / भर्ती पद" at the same position — previously downloaded templates will be rejected on upload with a heading-mismatch error. OK?
3. **Print filters:** reuse the full member-index filter set (status/sport/category/level/text) like the Excel export, or minimal like coaches (status scope only)? **Default: same filters as the Excel export, so print == Excel for the same query.**

### Test plan
- Pest Feature: XOR 422 (both set, create + update); each alone passes; initial_rank stored on create/update; export column swap; import mapping + both-posting row error + template headings; print (200 + letterhead + headings match export order + filters respected + 403); cross-org.
- Pest Unit: `OfficialPrintLayout` rendering (letterhead + watermark present, org title escaping).
- Pest Browser: skipped per repo convention (manual browser testing).

> **WAIT for user approval before starting Gate 2.**

---

## Plan amendment — 2026-09-03 (user answers to open questions)

User decisions:
1. **No backfill** — server DB has no data; `initial_rank` starts NULL for everyone. Migration is add-column only.
2. **Old import templates are irrelevant** — users download the latest sample; heading change at same position is fine, no compatibility shim.
3. **Print format = medals print style.** Reference implementation: `resources/js/pages/reports/medals/print.tsx` (route `reports.medals.print`). The member listing print must follow this format, and the Excel export must carry the same columns.

**Amendments to the plan above:**

- **Member listing print is an Inertia print page, not raw HTML.** New route `GET /members/print` (`members.print`) → `MemberController::print` renders new page `resources/js/pages/members/print.tsx`, modeled directly on `reports/medals/print.tsx`:
  - letterhead (logo + "UP Police Sports Control Board" subtitle), doc title (`Member Listing`), `Printed: <IST timestamp>` meta line;
  - bordered table, dark blue header row, `S. No.` serial column, `tfoot` totals row (member count), section-footer note;
  - screen-only toolbar with Print button (`window.print` flow like the medals page);
  - same filter query params as `members.export` (Spatie QueryBuilder), so **print and Excel show the same rows for the same query** — this answers open question 3 with the export-parity option.
- **Drop** the `OfficialPrintLayout` extraction and the `CoachController`/`TournamentExportController` refactor — out of scope; their prints stay untouched.
- `members/index.tsx`: Print button next to Export opens `members.print` with the current filter state (new tab).
- Print page rows are built server-side with the same column keys/order as `MemberExportController::COLUMN_LABELS` (single shared source of truth so print == Excel by construction).

> **WAIT for user approval before starting Gate 2.**
