# P6-T36 — Print preview polish: name dedupe, full rank names, translation gaps

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only.

- **Task ID:** P6-T36
- **Branch:** `feat/p6-t36-print-preview-polish`
- **Started:** 2026-09-04
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
Four user-reported issues on the coach/member print preview pages:
1. Coach print showed a "Display name" row (member page never had one) — office print shows the official name only; row removed.
2. Name appeared at multiple places on screen (tab title, breadcrumb, page heading) — the page `<h1>` duplicated the breadcrumb; both pages' h1 now read "Print preview". Printed sheet shows the name exactly once (profile section) on both pages; letterheads carry no person name.
3. `rankLabel()` on coach print preferred `short_name` — office print uses full `name` first.
4. 23 `t()` keys on coach print-preview missing from `hi.json` (fell back to English in Hindi locale); member page had no missing keys, but "From rank"/"To rank" were mistranslated ("current rank"/"target rank") for promotion tables.

### Research
- Investigation agent report (agent-8): name occurrence table, short_name usage, missing-key list verified against `resources/lang/hi.json` (1291 keys, `translations[key] ?? key` fallback).
- `t('From rank')`/`t('To rank')` used in member promotions-tab, coach show/print-preview, coach-quick-view — all promotion contexts, safe to fix semantics.
- Frontend-only; `search-docs` not required.

### Files to touch
- `resources/js/pages/coaches/print-preview.tsx` — remove Display name row; rankLabel order; h1 text
- `resources/js/pages/members/print-preview.tsx` — h1 text
- `resources/lang/hi.json` — 23 new keys; fix From rank/To rank

### Risk + mitigation
- **Risk:** "From rank"/"To rank" semantics change elsewhere. **Mitigation:** grep confirmed all usages are promotion from/to rank contexts.

### Test plan
- No PHP changes. Verify: all `t('...')` literals on both print pages resolve in hi.json (scripted check); prettier/eslint/types:check clean.

> User approved 2026-09-04. Gate 2 done same session.

---

## Summary (Gate 3)

### What changed
- `coaches/print-preview.tsx` — removed "Display name" row from profile details; `rankLabel()` now `name ?? short_name ?? code`; toolbar h1 shows `t('Print preview')` instead of the coach name.
- `members/print-preview.tsx` — toolbar h1 shows `t('Print preview')` instead of member name (breadcrumb still carries the name).
- `hi.json` — 23 new Hindi keys (Address, Assigned at, Bio, Certifications, Coach profile record, Display name, Effective from/to, Event / Weight, Evidence, Expired at, Issued at, Issuer, Medals, Playable sports, Primary, Print options, Profile details, Promotions / rewards, Reference, Removed at, Service and contact, Team assignments); "From rank" → पूर्व रैंक, "To rank" → नई रैंक.

### Decisions
- Kept name in browser tab title + breadcrumb (navigation context is legitimate); removed only the duplicate page heading. Printed sheet unchanged except Display name row removal.
- Member page translation labels mostly route through its local `uiText` dictionary (bypasses hi.json by design) — verified no missing t() keys there.

### Verification
- Scripted check: zero missing `t()` literals on both print pages.
- Prettier, ESLint, `npm run types:check` clean on touched files.

### Definition of Done
- [x] types:check / prettier / eslint clean
- [x] Translation keys resolve

---

## Revision 1 — 2026-09-04

**Requested by user:** "in the playable sport section remove the Primary / Effective from / Effective to / Notes, and in certification remove Issued at / Expired at" (same PR, pre-merge)
**What changed:** `coaches/print-preview.tsx` — sports table now shows only Sport + Event / Weight; certifications table only Name + Type + Issuer.
**Why:** office print keeps only identifying columns; dates/notes stay available on the show page.
**Tests updated:** not applicable (frontend-only; prettier/eslint/types:check clean).
