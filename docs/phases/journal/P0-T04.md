# P0-T04 — NPM packages

> Follow [docs/TASK_LIFECYCLE.md](../../TASK_LIFECYCLE.md). Append-only.

- **Task ID:** P0-T04
- **Phase file:** [`docs/phases/P00-bootstrap.md`](../P00-bootstrap.md)
- **Branch:** `chore/p0-t04-npm-packages`
- **Started:** 2026-05-21
- **Status:** approved

---

## Plan (Gate 1)

### Scope
Install `recharts@^3` (the only missing package). Verify all other required packages are already present from the starter kit. React compiler already wired into `vite.config.ts`. Fix doc drift (Vite ^7→^8, laravel-vite-plugin ^2→^3, remove dropped packages from STACK.md).

### Boost MCP research (mandatory)
- No Boost queries needed — task is `npm install recharts` + a doc audit. No Laravel/Inertia/Pest patterns involved.
- Skills consulted: none triggered.

**Doc drift found:**
- STACK.md: `vite ^7.0` → actual `^8.0.0`. Fixed this task.
- STACK.md: `laravel-vite-plugin ^2.0` → actual `^3.0.0`. Fixed this task.
- STACK.md: `@tanstack/react-query`, `react-hook-form`, `zod` listed as frontend deps — all dropped (Inertia owns data flow). Removed this task.
- STACK.md: `recharts ^3.0 | P7` → installed P0-T04. Updated.
- TASKS.md line 24: mentions `vite@^7` — corrected.

### Files to touch
- `package.json` / `package-lock.json` — `recharts@^3` added
- `docs/STACK.md` — version corrections + dropped packages removed
- `docs/TASKS.md` — vite version note corrected

### Schema / endpoints / pages
_None._

### Risk + mitigation
- **Risk:** recharts peer deps conflict with React 19.
  **Mitigation:** `npm install` succeeded cleanly; `npm run build` passed.

### Test plan
_None — `npm run build` succeeding is the full DoD for a pure dependency install._

> **WAIT for user approval before starting Gate 2.**

---

## Summary (Gate 3)

### What changed
| File | Change |
|---|---|
| `package.json` / `package-lock.json` | `recharts@^3` installed |
| `docs/STACK.md` | Vite ^8, laravel-vite-plugin ^3, removed TanStack/RHF/zod rows, recharts phase P0-T04, added "NOT used" note for dropped frontend libs |
| `docs/TASKS.md` | Vite version note corrected on P0-T04 line |

### Decisions
- **No version pin on `recharts`** beyond `^3` — patch/minor updates are safe and Renovate will manage them.
- **Doc drift fixed in-task** per TASK_LIFECYCLE Gate 1 rule: flag + fix in the same task.

### Tests added
_None — pure tooling task. DoD verified by `npm run build` (✓ built in 2.80s)._

### Deferred / follow-ups
- Large chunk warning (`app-BV1WSPhE.js` 547 kB) — defer to a dedicated code-splitting pass once the app has real pages. Not a build failure.

### Definition of Done
- [x] Code compiles / type-checks
- [x] `npm run build` — ✓ built in 2.80s, 0 errors
- [ ] `php artisan migrate:fresh --seed` clean _(no PHP changes)_
- [ ] Feature test per new endpoint _(n/a)_
- [ ] Unit test per new Service/Action _(n/a)_
- [ ] Vitest / Pest tests _(n/a)_
- [x] `vendor/bin/pint --dirty` — nothing to format (no PHP changes)
- [x] `vendor/bin/phpstan analyse --memory-limit=512M` — no new errors

---

## Revisions

_None._
