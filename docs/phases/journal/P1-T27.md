# P1-T27 — Units CRUD

- **Task ID:** P1-T27
- **Phase file:** [`docs/phases/P01-reference-data.md`](../P01-reference-data.md)
- **Branch:** `feat/p1-t27-units-crud`
- **Started:** 2026-05-22
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
"Units: same" — identical CRUD slice as P1-T25/T26 but for the `units` table. Extra fields vs Sports: `unit_type` enum, `commandant` (nullable), `district_id` (nullable FK). No slug, no unique constraint beyond PK.

### Boost MCP research (mandatory)
- `database-schema` table=`units` → id, organization_id, name_hi, name_en, unit_type ENUM(PAC|GRP|DISTRICT|HQ|OTHER), commandant nullable, district_id nullable FK, timestamps
- Pattern sourced from P1-T25/T26 codebase — confirmed exact patterns
- Skills consulted: `laravel-best-practices`, `inertia-react-development`, `wayfinder-development`, `pest-testing`

**Doc drift found:** None.

### Files to touch
- `app/Http/Requests/Settings/StoreUnitRequest.php` — create
- `app/Http/Requests/Settings/UpdateUnitRequest.php` — create
- `app/Policies/UnitPolicy.php` — create (stub exists from P1-T15, will be overwritten)
- `app/Http/Controllers/Settings/UnitController.php` — create
- `routes/settings.php` — add units resource route
- `resources/js/pages/settings/units/index.tsx` — create
- `resources/js/pages/settings/units/create.tsx` — create
- `resources/js/pages/settings/units/edit.tsx` — create
- `tests/Feature/Settings/UnitCrudTest.php` — create

### Key notes
- `district_id` nullable FK → controller passes `districts` prop to create/edit pages
- Index eager-loads `district` relation for display
- No slug generation
- Districts are global (no organization_id)

### Test plan
- index 200 admin, guest 302
- create 200 admin, guest 302
- store creates + redirects, validates required, validates enum, 403 no-permission
- store with district_id works
- edit 200, update redirects, update 403 cross-org (regular user)
- destroy deletes + redirects, 403 no-permission, 403 cross-org (regular user)

> **WAIT for user approval before starting Gate 2.**

---

## Summary (Gate 3)

_TBD_
