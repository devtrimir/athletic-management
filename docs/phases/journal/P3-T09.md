# P3-T09 + P3-T10 — Pest Feature: CoachController tests

- **Task ID:** P3-T09, P3-T10
- **Phase file:** [`docs/phases/P03-coaches.md`](../P03-coaches.md)
- **Branch:** `feat/p3-t09-t10-coach-controller-tests`
- **Started:** 2026-05-24
- **Status:** merged

---

## Plan (Gate 1)

### Scope
- **T09:** Pest Feature tests for CoachController CRUD + linked-vs-standalone behavior
- **T10:** Pest Feature: editing a linked coach's name does not mutate the linked member

Both bundled in one PR since they share the same test file and helper setup.

### Boost MCP research (mandatory)
- `search-docs` packages=[pestphp/pest] queries=["feature test inertia", "assertInertia"] → confirmed `assertInertia` with component assertion
- Skills consulted: pest-testing SKILL.md

**Doc drift found:** None.

### Files to touch
- `tests/Feature/CoachControllerTest.php` — new (covers T09 + T10)
- `docs/TASKS.md` — tick P3-T09, P3-T10

### Test plan
- T09: index returns 200 + paginated coaches; unauthenticated redirects; 403 without permission; store creates coach; update patches; destroy soft-deletes; standalone vs linked coach show
- T10: updating `full_name_hi` on a linked coach does not change `members.full_name_hi`

---

## Summary (Gate 3)

### What changed
- `tests/Feature/CoachControllerTest.php` — `coachUser()` helper; tests for all 7 CRUD actions + linked/standalone distinction + name isolation

### Decisions
- **`coachUser()` helper:** creates user with specified permission codes via `Permission::firstOrCreate`
- **T09+T10 bundled:** both test the same controller, no reason to split PRs

### Tests added
- `tests/Feature/CoachControllerTest.php` — ~20 tests

### Definition of Done
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [x] Authorization gate / Policy tested

---

## Approval (Gate 5)

## Approved — 2026-05-24
**PR:** #79 — feat/p3-t09-t10-coach-controller-tests
**Squash commit:** bundled with P3-T08 show page
**Merged:** 2026-05-24
