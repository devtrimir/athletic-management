# P5-T01 — Migration: `tournaments` (+ soft-delete, indexes)

- **Task ID:** P5-T01
- **Phase file:** (P05 phase file not yet created)
- **Branch:** `feat/p5-t01-t02-tournaments-events-schema`
- **Started:** 2026-05-24
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
"Migration: `tournaments` (+ soft-delete, indexes)." Creates the `tournaments` table per PHASED_REQUIREMENTS §Phase 5 schema spec. Bundled with P5-T02 (events) as both are pure schema tasks (2 migrations ≤ limit).

### Boost MCP research (mandatory)
- `database-schema` table=`tournament_tiers` → confirmed columns: id, code (enum 6 values), label_hi, label_en, weight, timestamps. FK target is valid.
- `database-schema` table=`sport_sessions` → organization_id present. Session FK is org-scoped.
- Convention check: sibling migration `create_teams_table.php` → `foreignId().constrained()` pattern, `softDeletes()` before `timestamps()`, named composite index via `$table->index([...])`.

**Doc drift found:** None.

### Files to touch
- `database/migrations/2026_05_24_115535_create_tournaments_table.php` — full schema
- `app/Models/Tournament.php` — Tenanted + SoftDeletes + relationships + PHPDoc
- `database/factories/TournamentFactory.php` — definition() + forOrganization() state
- `tests/Feature/TournamentsMigrationTest.php` — 7 tests

### Schema
- Columns: id, organization_id, session_id, tier_id, sport_id NULLABLE, name_hi, venue NULLABLE, date_from NULLABLE, date_to NULLABLE, raw_date_text NULLABLE, deleted_at, timestamps
- Indexes: composite (session_id, tier_id)
- Model traits: Tenanted (has organization_id), SoftDeletes, HasFactory

### Risk + mitigation
- **Risk:** TournamentTierFactory uses `fake()->unique()` → could collide across tests.
  **Mitigation:** `TournamentFactory` uses `TournamentTier::firstOrCreate(['code' => 'NATIONAL'])` — idempotent, no collision.

### Test plan
- `tournaments` table exists
- All 13 required columns present
- `sport_id` nullable
- Optional text fields (venue, date_from, date_to, raw_date_text) nullable
- Soft delete + restore works
- `forOrganization` factory state creates consistent org chain
- Tenanted scope filters by authenticated user's org

> **Approved at Gate 1. Proceeding to Gate 2.**

---

## Summary (Gate 3)

### What changed
- `database/migrations/2026_05_24_115535_create_tournaments_table.php` — full schema with nullable sport_id, nullable date/text fields, soft-deletes, composite index on (session_id, tier_id)
- `app/Models/Tournament.php` — Tenanted + SoftDeletes + 5 relationships (organization, session, tier, sport, events) + Fillable + casts
- `database/factories/TournamentFactory.php` — definition() with firstOrCreate tier; forOrganization() state
- `tests/Feature/TournamentsMigrationTest.php` — 7 passing tests

### Decisions
- **TournamentTier in factory:** Used `firstOrCreate` with NATIONAL tier rather than `TournamentTier::factory()` to avoid unique-code collisions when multiple tests in the same process create tiers.
- **`sport_id` left null in forOrganization state:** Sport is optional on tournaments per spec; tests don't require it.
