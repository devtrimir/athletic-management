# P7-T18 — Tournament Show Page Modals

> Copy this file to `docs/phases/journal/P7-T18.md` at the start of every task.

- **Task ID:** P7-T18
- **Branch:** `feat/p7-t18-tournament-show-modals`
- **Started:** 2026-05-27
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
"Tournament show page UX — remove inline `AddEventPanel`; add `AddEventDialog` / `EditEventDialog` / `ConfirmDeleteDialog` modals; per-row edit+delete on events table; confirm modal on tournament delete; backend: `PATCH` + `DELETE` for `EventController`"

### Boost MCP research (mandatory)
- `search-docs` packages=[@inertiajs/react, laravel/framework, pestphp/pest] queries=["useForm delete method confirmation", "inertia router delete confirmation dialog", "AlertDialog confirm shadcn"] → Found Dialog/AlertDialog patterns; using Dialog-based confirm since alert-dialog not in project; PATCH/DELETE routes use standard `Route::patch()` / `Route::delete()`
- `database-schema` not needed — no migrations
- Skills consulted: `inertia-react-development`, `laravel-best-practices`, `pest-testing`

**Doc drift found:** None

### Files to touch
- `routes/web.php` — add PATCH + DELETE routes for events
- `app/Http/Requests/Events/UpdateEventRequest.php` — new, same rules as Store
- `app/Http/Controllers/EventController.php` — add `update()` + `destroy()`
- `resources/js/pages/tournaments/show.tsx` — full rewrite: remove AddEventPanel inline form; add AddEventDialog, EditEventDialog, ConfirmDeleteDialog modals; per-row edit+delete; confirm on tournament delete
- `tests/Feature/EventControllerTest.php` — add update + destroy tests
- `resources/lang/hi.json` — new translation strings

### Schema / endpoints / pages
- Endpoint: `PATCH tournaments/{tournament}/events/{event}` → `EventController@update`
- Endpoint: `DELETE tournaments/{tournament}/events/{event}` → `EventController@destroy`

### Risk + mitigation
- **Risk:** `EventController@destroy` deletes participations via cascade — acceptable, same as tournament delete
  **Mitigation:** flash a clear confirmation message

### Test plan
- Pest Feature: event update (success, validation, auth, policy), event destroy (success, auth, policy, redirects to tournament show)

> **User approved Gate 1 on 2026-05-27.**
