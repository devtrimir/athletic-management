# P7P-T01 — Participation Awards + Scoring Config Foundation

- **Task ID:** P7P-T01
- **Phase file:** [`docs/phases/P07-player-performance.md`](../P07-player-performance.md)
- **Branch:** `feat/p7p-t01-performance-foundation`
- **Started:** 2026-06-13
- **Status:** implementing

---

## Plan (Gate 1)

### Scope
Add the missing award concept for player performance scoring by introducing `participation_awards`, wiring the corresponding model / relation / factory, and defining initial scoring rules in `config/player_points.php`.

### Boost MCP research (mandatory)
- `search-docs` packages=`laravel/framework,inertiajs/inertia-laravel,@inertiajs/react` queries=`configuration files custom config service container`, `database migrations enum json report services collections`, `inertia reports filters table pages` → config-backed rules and conventional migration/model structure fit the current stack well; no framework feature blocks this approach.
- Skills consulted: `laravel-best-practices`, `pest-testing`

**Doc drift found:** None.

### Files to touch
- `docs/TASKS.md` — add the new Phase 7P task slice
- `docs/phases/P07-player-performance.md` — detailed plan / sequencing
- `database/migrations/..._create_participation_awards_table.php` — new awards table
- `app/Models/ParticipationAward.php` — model
- `app/Models/Participation.php` — relation
- `database/factories/ParticipationAwardFactory.php` — test data
- `config/player_points.php` — initial scoring rules
- `tests/Feature/ParticipationAwardFoundationTest.php` — migration/model/config coverage

### Schema / endpoints / pages
- Migration: `create_participation_awards_table` — org-scoped award records attached to participations

### Risk + mitigation
- **Risk:** confusing medals with awards and duplicating semantics
  **Mitigation:** keep awards in a separate table and leave `MERIT` unchanged in achievements

### Test plan
- Pest Feature: table exists with expected columns
- Pest Feature: participation can own awards
- Pest Feature: config exposes the expected initial scoring keys

---

## Summary (Gate 3)

### What changed
- `docs/TASKS.md` — added Phase `7P` task slice for the player performance points system
- `docs/phases/P07-player-performance.md` — wrote the detailed phase plan and sequencing
- `database/migrations/2026_06_13_092505_create_participation_awards_table.php` — added the org-scoped awards table linked to participations
- `app/Models/ParticipationAward.php` — added the tenanted, auditable award model with fillable fields and casts
- `app/Models/Participation.php` — added the `participationAwards()` relation
- `database/factories/ParticipationAwardFactory.php` — added aligned org/participation award test data
- `config/player_points.php` — added the initial additive scoring rules for participation, medals, tiers, and awards
- `tests/Feature/ParticipationAwardFoundationTest.php` — covers migration columns, relation wiring, and config defaults

### Decisions
- **Awards stay separate from medals:** `BEST_PLAYER` and similar titles live in `participation_awards`; `MERIT` remains an achievement medal type.
- **Rules start in config:** scoring is defined in `config/player_points.php` for auditability and faster iteration before any admin editor exists.

### Tests added
- `tests/Feature/ParticipationAwardFoundationTest.php` — 4 passing tests covering schema, relation, and config

### Deferred / follow-ups
- `P7P-T02` — build `PlayerPointsService` on top of these new source records

### Definition of Done
- [ ] Code compiles / type-checks
- [ ] `php artisan migrate:fresh --seed` clean
- [ ] `php artisan migrate:rollback` clean on new migrations
- [x] Feature test for the new foundation behavior
- [ ] Unit test per new Service/Action
- [ ] Vitest test per non-trivial component
- [ ] Pest Browser happy-path (if user-facing)
- [x] `php artisan test` passes
- [x] `vendor/bin/pint --test` passes
- [ ] `vendor/bin/phpstan analyse` passes
- [ ] `npm run lint` passes
- [ ] `npm run type-check` passes
- [ ] `npm run build` passes
- [ ] Authorization gate / Policy added or updated
- [ ] Audit log fires (if applicable)
- [ ] No new hard-coded user-facing strings (from P9 onward)
- [x] Phase file route/page table updated
- [ ] ADR filed if non-obvious decision made
- [ ] Screenshots attached (UI changes)
